# Zweisprachigkeit (i18n) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Alpenpfad wird zweisprachig (DE default, EN zweite aktive Sprache, N-Sprachen-ready). Frontend-UI + Directus-Content werden lokalisiert, Navigation aus den statischen Komponenten nach Directus gezogen und übersetzt.

**Architektur:** Drei lose gekoppelte Schichten. (1) **Directus:** `languages`-Collection aktiviert, 15 Translations-Subtables für skalare Felder, 7 neue Item-Sub-Collections für ehemalige JSON-Arrays (je mit eigenen Translations), Navigation-Records seeded. (2) **Nuxt Server:** Content-APIs akzeptieren `?locale=de|en`, filtern Directus-Translations via `deep`, geben 404 bei fehlender Translation; neue Endpoints für Navigation und Languages. (3) **Nuxt Frontend:** `@nuxtjs/i18n@9` mit `strategy: 'prefix'`, Locale-Files für UI-Strings, Slug-Translation via `definePageMeta.i18n.paths`, Language-Switcher-Composable, Zod-i18n-map für Formular-Fehler.

**Tech-Stack:** Nuxt 4.4, Vue 3 `<script setup>`, TypeScript, Directus 11.6.1 + `@directus/sdk` 21.x, `@nuxtjs/i18n@9`, `zod-i18n-map`, `zod` 3.x (bereits vorhanden), Tailwind v4, Node 22+ Scripts (native `fetch`), shadcn-vue.

**Reference-Spec:** [backup/docs/superpowers/specs/2026-04-23-i18n-design.md](../specs/2026-04-23-i18n-design.md)

**Testing-Ansatz:** Identisch zu Auth und Buchungen — **kein Test-Framework**. Verification läuft über: `yarn build` (Typ-Check-Gate), Script-Idempotenz (zweiter Lauf = identischer Output), curl-Smoketests für API-Routes, manuelle Browser-Tests für UI. **Kein Test-Framework aufsetzen.**

**Working-Directory für alle Commands:** `backup/` (Nuxt-Projekt-Root).

**Verification-Gate:** `yarn build 2>&1 | tail -5` muss grün sein. **Nicht** `yarn nuxi typecheck` (pre-existing vue-tsc-Probleme laut HANDOFF).

**Commit-Disziplin:** Ausschließlich explizite Pfade stagen. **Niemals** `git add -A` oder `git add .` — [backup/.env.example](../../../.env.example) hat eine pre-existing Modifikation, die nicht committed werden darf.

**Directus-Zugang:** Token + URL liegen in [backup/.env](../../../.env). In Scripts via `process.env.DIRECTUS_TOKEN` / `process.env.DIRECTUS_URL`.

---

## File Structure

**Neue Dateien:**
```
backup/
├── app/
│   ├── locales/
│   │   ├── de.json                                  (UI-Strings DE)
│   │   └── en.json                                  (UI-Strings EN)
│   ├── plugins/
│   │   └── zod-i18n.ts                              (Zod-ErrorMap global)
│   ├── composables/
│   │   ├── useAlternateLocales.ts                   (Slug-Translations für Switcher)
│   │   └── useNavigation.ts                         (Navigation-Fetch mit Locale)
│   └── components/
│       └── Website/
│           ├── LanguageSwitcher.vue                 (DE/EN Link-Umschalter)
│           ├── Footer.vue                           (Directus-basiert, mehrsprachig)
│           └── FooterColumn.vue                     (eine Spalte des Footers)
├── server/
│   ├── api/
│   │   └── content/
│   │       ├── navigation.get.ts                    (3 Navigations-Records mit Translations)
│   │       └── languages.get.ts                     (Liste aktiver Sprachen)
│   └── utils/
│       └── i18n.ts                                  (Locale-Mapping + Directus-Helper)
├── scripts/
│   ├── i18n-setup.mjs                               (Haupt-Setup-Script)
│   └── i18n-content-en.mjs                          (EN-Entwürfe als Datenmodul)
└── docs/superpowers/plans/
    └── 2026-04-23-i18n.md                           (dieses Dokument)
```

**Modifizierte Dateien:**
```
backup/
├── nuxt.config.ts                                   (i18n-Modul + locales-Konfig)
├── package.json                                     (+ @nuxtjs/i18n, zod-i18n-map, script-entry)
├── app/
│   ├── components/
│   │   ├── Website/
│   │   │   ├── Header.vue                           (Stack-Umbau, Server-API, kein Pinia)
│   │   │   ├── MainMenu.vue                         (Translation-aware, ohne facilities)
│   │   │   └── MenuItem.vue                         (übersetzter title + sprach-abhängige Hrefs)
│   │   ├── Auth/LoginForm.vue                       ($t(), zod-i18n-map)
│   │   ├── Auth/RegisterForm.vue                    (desgleichen)
│   │   ├── Auth/PasswordResetForm.vue               (desgleichen)
│   │   ├── Auth/PasswordResetRequestForm.vue        (desgleichen)
│   │   ├── Auth/ProfileForm.vue                     (desgleichen)
│   │   ├── Auth/UserMenu.vue                        ($t())
│   │   ├── Auth/UserAvatar.vue                      ($t() für aria-label)
│   │   ├── Buchung/BuchungForm.vue                  ($t(), zod-i18n-map)
│   │   ├── Buchung/BuchungCard.vue                  ($t(), status-i18n)
│   │   ├── Buchung/BuchungDetail.vue                ($t())
│   │   ├── Buchung/BuchungStatusBadge.vue           ($t())
│   │   ├── Tour/Hero.vue                            ($t())
│   │   ├── Tour/CTA.vue                             ($t())
│   │   ├── Tour/FactsBar.vue                        ($t())
│   │   ├── Tour/Highlights.vue                      ($t())
│   │   ├── Tour/Included.vue                        ($t())
│   │   ├── Tour/Organizational.vue                  ($t())
│   │   ├── Tour/Gallery.vue                         ($t() für aria-labels)
│   │   ├── Tour/StickyMobileCTA.vue                 ($t())
│   │   ├── Tour/TourTermine.vue                     ($t())
│   │   └── sections/
│   │       ├── SectionHero.vue                      ($t())
│   │       ├── SectionStats.vue                     (Items aus Directus statt hardcoded)
│   │       ├── SectionTours.vue                     ($t())
│   │       ├── SectionBenefits.vue                  (Items aus Directus)
│   │       ├── SectionRegions.vue                   (Regions aus Directus)
│   │       ├── SectionTestimonials.vue              (Items aus Directus)
│   │       └── SectionNewsletter.vue                ($t())
│   ├── pages/
│   │   ├── index.vue                                ($t(), useSeoMeta lokalisiert)
│   │   ├── static.vue                               ($t())
│   │   ├── anmelden.vue                             (definePageMeta.i18n.paths, $t())
│   │   ├── registrieren.vue                         (desgleichen)
│   │   ├── passwort-vergessen.vue                   (desgleichen)
│   │   ├── passwort-ruecksetzen.vue                 (desgleichen)
│   │   ├── konto/index.vue                          (desgleichen)
│   │   ├── konto/passwort.vue                       (desgleichen)
│   │   ├── konto/buchungen/index.vue                (desgleichen)
│   │   ├── konto/buchungen/[id].vue                 (desgleichen)
│   │   ├── touren/[slug]/index.vue                  (locale-abhängiger Slug aus API, $t())
│   │   ├── touren/[slug]/buchen.vue                 (desgleichen)
│   │   ├── [...slug].vue                            (pages-catch-all translation-aware)
│   │   └── error.vue                                (neu: lokalisierte 404, falls nicht existiert)
│   └── app.vue                                      (LanguageSwitcher, falls global)
└── server/api/content/
    ├── homepage.get.ts                              (+ locale-Param + Translation-Filter)
    ├── page.get.ts                                  (desgleichen)
    ├── tour.get.ts                                  (+ alternate_locales-Map)
    └── block.get.ts                                 (+ Deep-Join für Item-Sub-Collections)
```

**Gelöschte Dateien:**
```
backup/app/components/sections/
├── TheHeader.vue                                    (ersetzt durch components/Website/Header.vue)
└── TheFooter.vue                                    (ersetzt durch components/Website/Footer.vue)
```

**Backup-Artefakte (erzeugt in Task 1):**
```
backup/exports/migrations/
├── 2026-04-23-i18n-touren.json
├── 2026-04-23-i18n-tour_termine.json
├── 2026-04-23-i18n-pages.json
├── 2026-04-23-i18n-seo.json
├── 2026-04-23-i18n-block_heroBanner.json
├── 2026-04-23-i18n-block_statsBand.json
├── 2026-04-23-i18n-block_tourGrid.json
├── 2026-04-23-i18n-block_benefits.json
├── 2026-04-23-i18n-block_regionList.json
├── 2026-04-23-i18n-block_testimonials.json
├── 2026-04-23-i18n-block_newsletter.json
├── 2026-04-23-i18n-block_carousel.json
├── 2026-04-23-i18n-block_imageText.json
├── 2026-04-23-i18n-block_text.json
├── 2026-04-23-i18n-block_banner.json
├── 2026-04-23-i18n-navigation.json
└── 2026-04-23-i18n-navigation_items.json
```

---

## Phase 1 — Backend Schema + Content Migration

Directus bekommt die vollständige i18n-Schema-Struktur. Frontend bleibt unangetastet; Server-APIs liefern ohne `locale`-Param weiterhin DE (Default). Am Ende dieser Phase läuft das bestehende Frontend unverändert, aber im Directus-Admin sehen alle betroffenen Collections die Language-Tabs und die Items liegen als Drag-and-Drop-Listen statt JSON-Editoren.

### Task 1: Setup-Script-Skelett + Backup-Mechanismus

Erzeugt die Infrastruktur: CLI-Parsing, Directus-Client, Backup-Funktion. Keine Schema-Änderungen. Nur Backups der 17 betroffenen Collections nach `exports/migrations/`.

**Files:**
- Create: `scripts/i18n-setup.mjs`
- Create: `scripts/directus-admin.mjs` (shared helper, falls noch nicht existent — sonst importieren)
- Modify: `package.json` (yarn-script-entry `i18n:setup`)

- [ ] **Step 1: Shared Directus-Client**

Prüfen, ob `scripts/directus.mjs` existiert ([siehe Auth-Setup-Pattern](../../../scripts/auth-setup.mjs)). Falls ja, importieren. Falls nein, anlegen:

```javascript
// backup/scripts/directus.mjs
import { createDirectus, rest, staticToken } from '@directus/sdk'
import 'dotenv/config'

const url = process.env.DIRECTUS_URL
const token = process.env.DIRECTUS_TOKEN
if (!url || !token) {
  console.error('DIRECTUS_URL or DIRECTUS_TOKEN missing in .env')
  process.exit(1)
}
export const directus = createDirectus(url).with(staticToken(token)).with(rest())
```

- [ ] **Step 2: Script-Skelett schreiben**

Datei [backup/scripts/i18n-setup.mjs](../../../scripts/i18n-setup.mjs) anlegen:

```javascript
/**
 * i18n-Setup: Zweisprachigkeit (DE/EN) im Directus aufbauen.
 * Idempotent — kann beliebig oft laufen.
 *
 * Usage:
 *   node scripts/i18n-setup.mjs            (voller Lauf)
 *   node scripts/i18n-setup.mjs --dry-run  (zeigt nur geplante Änderungen)
 *   node scripts/i18n-setup.mjs --rollback (stellt Backup wieder her)
 */
import { readItems } from '@directus/sdk'
import fs from 'node:fs/promises'
import path from 'node:path'
import { directus } from './directus.mjs'

const DRY = process.argv.includes('--dry-run')
const ROLLBACK = process.argv.includes('--rollback')
const BACKUP_DIR = path.resolve('exports/migrations')
const BACKUP_DATE = '2026-04-23'

const COLLECTIONS_TO_BACKUP = [
  'touren', 'tour_termine', 'pages', 'seo',
  'block_heroBanner', 'block_statsBand', 'block_tourGrid', 'block_benefits',
  'block_regionList', 'block_testimonials', 'block_newsletter', 'block_carousel',
  'block_imageText', 'block_text', 'block_banner',
  'navigation', 'navigation_items',
]

async function backupCollection(name) {
  const file = path.join(BACKUP_DIR, `${BACKUP_DATE}-i18n-${name}.json`)
  try {
    await fs.access(file)
    console.log(`  ✓ backup exists: ${name}`)
    return
  } catch {}
  const data = await directus.request(readItems(name, { limit: -1 }))
  if (DRY) {
    console.log(`  (dry) would write ${file} (${data.length} records)`)
    return
  }
  await fs.writeFile(file, JSON.stringify(data, null, 2), 'utf8')
  console.log(`  + backup: ${name} (${data.length} records)`)
}

async function run() {
  console.log(`--- i18n-setup ${DRY ? '(DRY RUN)' : ''} ---\n`)
  await fs.mkdir(BACKUP_DIR, { recursive: true })

  console.log('→ Backup')
  for (const c of COLLECTIONS_TO_BACKUP) {
    await backupCollection(c)
  }

  if (ROLLBACK) {
    console.log('\nRollback not yet implemented — manual restore from exports/migrations/')
    process.exit(0)
  }

  // Tasks 2–7 hängen hier weitere Schritte an:
  // await ensureLanguagesCollection()
  // await ensureContentTranslations()
  // await ensureBlockTranslations()
  // await ensureItemSubCollections()
  // await migrateData()
  // await seedNavigation()
  // await ensurePermissions()
  // await printReport()

  console.log('\n--- done ---')
}

run().catch((e) => { console.error(e); process.exit(1) })
```

- [ ] **Step 3: Yarn-Script-Entry ergänzen**

In [backup/package.json](../../../package.json) unter `"scripts"`:

```json
"i18n:setup": "node scripts/i18n-setup.mjs"
```

- [ ] **Step 4: Dry-Run starten**

```bash
cd backup && yarn i18n:setup --dry-run 2>&1 | head -30
```

Erwartet: Liste der 17 Collections mit `(dry) would write …`. Kein echtes File entsteht.

- [ ] **Step 5: Echten Backup-Lauf**

```bash
cd backup && yarn i18n:setup 2>&1 | head -30
```

Erwartet: 17 Backup-Files in `exports/migrations/`, jedes mit JSON-Array (kann leer `[]` sein). Zweiter Lauf: `✓ backup exists` für alle.

- [ ] **Step 6: Verifikation**

```bash
ls backup/exports/migrations/2026-04-23-i18n-*.json | wc -l
# Erwartet: 17
```

- [ ] **Step 7: Commit**

```bash
git add backup/scripts/i18n-setup.mjs backup/package.json
# falls directus.mjs neu angelegt:
git add backup/scripts/directus.mjs
# Backup-Files sind schon in .gitignore (exports/ sollte nicht versioniert werden — prüfen)
git commit -m "feat(i18n): add setup-script skeleton with backup mechanism"
```

Wenn [backup/.gitignore](../../../.gitignore) `exports/` nicht enthält, hinzufügen (sie dienen nur als lokale Rollback-Reserve):

```bash
echo "exports/migrations/" >> backup/.gitignore
git add backup/.gitignore
git commit --amend --no-edit
```

### Task 2: Languages-Collection aktivieren + Permissions

Legt die `languages`-Collection mit `de-DE` und `en-US` an und erweitert die `Kunde`-Policy um `read`-Permission.

**Files:**
- Modify: `scripts/i18n-setup.mjs` (Funktion `ensureLanguagesCollection`)

- [ ] **Step 1: Helper-Funktionen hinzufügen**

Im Script vor `run()`:

```javascript
import {
  readCollections, createCollection, readFieldsByCollection, createField,
  readItems as readItemsSdk, createItem, readPolicies, readPermissions, createPermission,
} from '@directus/sdk'

async function ensureLanguagesCollection() {
  const cols = await directus.request(readCollections())
  const exists = cols.find((c) => c.collection === 'languages')
  if (!exists) {
    if (DRY) { console.log('  (dry) would create languages collection'); return }
    await directus.request(createCollection({
      collection: 'languages',
      meta: { icon: 'translate', note: 'Aktive Frontend-Sprachen', hidden: false, singleton: false },
      schema: {},
      fields: [
        { field: 'code', type: 'string', schema: { is_primary_key: true, is_nullable: false, max_length: 8 },
          meta: { interface: 'input', required: true, width: 'half' } },
        { field: 'name', type: 'string', schema: { is_nullable: false, max_length: 64 },
          meta: { interface: 'input', required: true, width: 'half' } },
        { field: 'direction', type: 'string', schema: { default_value: 'ltr', is_nullable: false, max_length: 3 },
          meta: { interface: 'select-dropdown', options: { choices: [{ text: 'LTR', value: 'ltr' }, { text: 'RTL', value: 'rtl' }] } } },
      ],
    }))
    console.log('  + languages collection')
  } else {
    console.log('  ✓ languages collection')
  }

  const seeds = [
    { code: 'de-DE', name: 'Deutsch', direction: 'ltr' },
    { code: 'en-US', name: 'English', direction: 'ltr' },
  ]
  const existing = await directus.request(readItemsSdk('languages', { limit: -1 }))
  for (const s of seeds) {
    if (existing.find((e) => e.code === s.code)) {
      console.log(`    ✓ ${s.code}`)
      continue
    }
    if (DRY) { console.log(`    (dry) would seed ${s.code}`); continue }
    await directus.request(createItem('languages', s))
    console.log(`    + ${s.code}`)
  }
}

async function ensureLanguagesPermission() {
  const policies = await directus.request(readPolicies({ filter: { name: { _eq: 'Kunde' } }, limit: 1 }))
  if (!policies.length) {
    console.log('  ⚠ Kunde-Policy nicht gefunden (Auth-Setup noch nicht gelaufen?) — überspringe')
    return
  }
  const policyId = policies[0].id
  const perms = await directus.request(readPermissions({
    filter: { policy: { _eq: policyId }, collection: { _eq: 'languages' }, action: { _eq: 'read' } },
    limit: 1,
  }))
  if (perms.length) {
    console.log('  ✓ read-permission on languages')
    return
  }
  if (DRY) { console.log('  (dry) would add read-permission on languages'); return }
  await directus.request(createPermission({
    policy: policyId, collection: 'languages', action: 'read', fields: ['*'], permissions: {}, validation: {},
  }))
  console.log('  + read-permission on languages')
}
```

Im `run()` nach dem Backup-Block:

```javascript
  console.log('\n→ Languages')
  await ensureLanguagesCollection()
  await ensureLanguagesPermission()
```

- [ ] **Step 2: Lauf**

```bash
cd backup && yarn i18n:setup 2>&1 | tail -15
```

Erwartet: `+ languages collection` + `+ de-DE` + `+ en-US` + `+ read-permission on languages` (beim zweiten Lauf alle mit `✓`).

- [ ] **Step 3: Smoketest**

```bash
TOKEN=$(grep '^DIRECTUS_TOKEN=' backup/.env | cut -d= -f2)
URL=$(grep '^DIRECTUS_URL=' backup/.env | cut -d= -f2)
curl -s -H "Authorization: Bearer $TOKEN" "$URL/items/languages" | head -c 200
```

Erwartet: JSON mit 2 Records (`de-DE`, `en-US`).

- [ ] **Step 4: Commit**

```bash
git add backup/scripts/i18n-setup.mjs
git commit -m "feat(i18n): add languages collection with DE/EN + policy read perm"
```

### Task 3: Translation-Subtables für Stammdaten (touren, tour_termine, pages, seo)

Legt `<collection>_translations`-Subtables an, verschiebt übersetzbare Felder aus der Haupttabelle in die Subtable, setzt Unique-Constraints, migriert bestehende DE-Daten als Translation-Records.

**Files:**
- Modify: `scripts/i18n-setup.mjs` (Funktion `ensureContentTranslations`)

- [ ] **Step 1: Feldkatalog einfügen**

Im Script als Top-Level-Konstante (pro Stammdaten-Collection die Felder auflisten, die in die Translation wandern):

```javascript
// Format: { <collection>: { scalar: [translatable fields...], hasSlug: bool } }
const CONTENT_TRANSLATIONS = {
  touren: {
    fields: [
      { field: 'slug',         type: 'string', schema: { is_nullable: false } },
      { field: 'title',        type: 'string', schema: { is_nullable: false } },
      { field: 'subtitle',     type: 'string', schema: {} },
      { field: 'intro',        type: 'text',   schema: {} },
      { field: 'highlights',   type: 'json',   schema: {}, meta: { interface: 'tags', special: ['cast-json'] } },
      { field: 'included',     type: 'json',   schema: {}, meta: { interface: 'tags', special: ['cast-json'] } },
      { field: 'not_included', type: 'json',   schema: {}, meta: { interface: 'tags', special: ['cast-json'] } },
      { field: 'meeting_point', type: 'string', schema: {} },
      { field: 'season',       type: 'string', schema: {} },
    ],
    hasSlug: true,
  },
  tour_termine: {
    fields: [
      { field: 'hinweis', type: 'string', schema: {} },
    ],
    hasSlug: false,
  },
  pages: {
    fields: [
      { field: 'slug',  type: 'string', schema: { is_nullable: false } },
      { field: 'title', type: 'string', schema: {} },
    ],
    hasSlug: true,
  },
  seo: {
    fields: [
      { field: 'title',            type: 'text', schema: {} },
      { field: 'meta_description', type: 'text', schema: {} },
    ],
    hasSlug: false,
  },
}
```

- [ ] **Step 2: Schema-Aufbau-Funktion**

```javascript
import { createRelation, updateField, deleteField } from '@directus/sdk'

async function ensureTranslationsSubtable(parent, cfg) {
  const subtable = `${parent}_translations`
  const cols = await directus.request(readCollections())
  if (cols.find((c) => c.collection === subtable)) {
    console.log(`  ✓ ${subtable}`)
    return
  }
  if (DRY) { console.log(`  (dry) would create ${subtable}`); return }

  // Parent-Collection-PK-Typ ermitteln (uuid oder integer)
  const parentFields = await directus.request(readFieldsByCollection(parent))
  const pk = parentFields.find((f) => f.schema?.is_primary_key)
  const pkType = pk?.type ?? 'uuid'

  await directus.request(createCollection({
    collection: subtable,
    meta: { hidden: true, singleton: false, icon: 'translate' },
    schema: {},
    fields: [
      { field: 'id', type: 'uuid',
        meta: { hidden: true, interface: 'input', readonly: true, special: ['uuid'] },
        schema: { is_primary_key: true, has_auto_increment: false } },
      { field: `${parent}_id`, type: pkType,
        meta: { hidden: true, interface: 'input', readonly: true },
        schema: {} },
      { field: 'languages_code', type: 'string',
        meta: { hidden: true, interface: 'input' }, schema: { max_length: 8 } },
      ...cfg.fields.map((f) => ({ ...f })),
    ],
  }))

  // Relations setzen
  await directus.request(createRelation({
    collection: subtable, field: `${parent}_id`, related_collection: parent,
    meta: { one_field: 'translations', sort_field: null, one_deselect_action: 'delete', junction_field: 'languages_code' },
    schema: { on_delete: 'CASCADE' },
  }))
  await directus.request(createRelation({
    collection: subtable, field: 'languages_code', related_collection: 'languages',
    meta: { one_field: null, sort_field: null, junction_field: `${parent}_id` },
    schema: { on_delete: 'SET NULL' },
  }))

  // Parent bekommt translations alias-field mit interface 'translations'
  await directus.request(createField(parent, {
    field: 'translations', type: 'alias',
    meta: { interface: 'translations', special: ['translations'], options: { languageField: 'code' } },
    schema: null,
  }))

  console.log(`  + ${subtable} (fields: ${cfg.fields.map((f) => f.field).join(', ')})`)
}

async function ensureContentTranslations() {
  for (const [parent, cfg] of Object.entries(CONTENT_TRANSLATIONS)) {
    await ensureTranslationsSubtable(parent, cfg)
  }
}
```

Im `run()`:

```javascript
  console.log('\n→ Content translations')
  await ensureContentTranslations()
```

- [ ] **Step 3: Lauf + Verifikation Schema**

```bash
cd backup && yarn i18n:setup 2>&1 | tail -20
```

Erwartet: `+ touren_translations`, `+ tour_termine_translations`, `+ pages_translations`, `+ seo_translations`.

Smoketest:
```bash
curl -s -H "Authorization: Bearer $TOKEN" "$URL/fields/touren_translations" -o /tmp/tt.json && node -e "const d=require('/tmp/tt.json').data; console.log(d.map(f=>f.field).join(','))"
```
Erwartet (Reihenfolge kann variieren): `id,touren_id,languages_code,slug,title,subtitle,intro,highlights,included,not_included,meeting_point,season`.

- [ ] **Step 4: Commit**

```bash
git add backup/scripts/i18n-setup.mjs
git commit -m "feat(i18n): add translations subtables for touren/tour_termine/pages/seo"
```

### Task 4: Translation-Subtables für die 10 skalaren Block-Collections

Gleiche Logik wie Task 3, aber für die 10 Blocks mit übersetzbaren skalaren Feldern (alle außer `block_statsBand` und `block_reception`).

**Files:**
- Modify: `scripts/i18n-setup.mjs`

- [ ] **Step 1: Block-Feldkatalog**

```javascript
const BLOCK_TRANSLATIONS = {
  block_heroBanner: [
    { field: 'title',               type: 'text',   schema: {} },
    { field: 'eyebrow',             type: 'string', schema: {} },
    { field: 'lead',                type: 'text',   schema: {} },
    { field: 'cta_primary_label',   type: 'string', schema: {} },
    { field: 'cta_secondary_label', type: 'string', schema: {} },
  ],
  block_tourGrid: [
    { field: 'eyebrow',   type: 'string', schema: {} },
    { field: 'headline',  type: 'string', schema: {} },
    { field: 'lead',      type: 'text',   schema: {} },
    { field: 'cta_label', type: 'string', schema: {} },
  ],
  block_benefits: [
    { field: 'eyebrow',  type: 'string', schema: {} },
    { field: 'headline', type: 'string', schema: {} },
    { field: 'lead',     type: 'text',   schema: {} },
  ],
  block_regionList: [
    { field: 'eyebrow',   type: 'string', schema: {} },
    { field: 'headline',  type: 'string', schema: {} },
    { field: 'lead',      type: 'text',   schema: {} },
    { field: 'cta_label', type: 'string', schema: {} },
  ],
  block_testimonials: [
    { field: 'eyebrow',  type: 'string', schema: {} },
    { field: 'headline', type: 'string', schema: {} },
  ],
  block_newsletter: [
    { field: 'eyebrow',       type: 'string', schema: {} },
    { field: 'headline',      type: 'string', schema: {} },
    { field: 'lead',          type: 'text',   schema: {} },
    { field: 'placeholder',   type: 'string', schema: {} },
    { field: 'cta_label',     type: 'string', schema: {} },
    { field: 'success_title', type: 'string', schema: {} },
    { field: 'success_text',  type: 'text',   schema: {} },
  ],
  block_carousel: [
    { field: 'title', type: 'text', schema: {} },
  ],
  block_imageText: [
    { field: 'text', type: 'text', schema: {} },
  ],
  block_text: [
    { field: 'content', type: 'text', schema: {}, meta: { interface: 'input-rich-text-html' } },
  ],
  block_banner: [
    { field: 'title', type: 'text', schema: {} },
  ],
}
```

- [ ] **Step 2: Ensure-Funktion aufrufen**

```javascript
async function ensureBlockTranslations() {
  for (const [parent, fields] of Object.entries(BLOCK_TRANSLATIONS)) {
    await ensureTranslationsSubtable(parent, { fields, hasSlug: false })
  }
}
```

Im `run()`:

```javascript
  console.log('\n→ Block translations')
  await ensureBlockTranslations()
```

- [ ] **Step 3: Lauf**

```bash
cd backup && yarn i18n:setup 2>&1 | tail -20
```

Erwartet: 10 neue `block_*_translations`-Subtables.

- [ ] **Step 4: Smoketest**

```bash
curl -s -H "Authorization: Bearer $TOKEN" "$URL/collections?limit=-1" -o /tmp/c.json && node -e "const d=require('/tmp/c.json').data; console.log(d.filter(c=>c.collection.endsWith('_translations')).map(c=>c.collection).sort().join('\n'))"
```
Erwartet: 14 Einträge (4 content + 10 blocks; `directus_translations` ist System und wird auch angezeigt).

- [ ] **Step 5: Commit**

```bash
git add backup/scripts/i18n-setup.mjs
git commit -m "feat(i18n): add translations subtables for 10 scalar content blocks"
```

### Task 5: Item-Sub-Collections + ihre Translations (7 Stück)

Legt 7 neue Item-Sub-Collections an (`block_statsBand_items`, `block_benefits_items`, `block_testimonials_items`, `block_regionList_regions`, `block_heroBanner_trust_signals`, `block_imageText_buttons`, `block_banner_buttons`), jede mit eigener `_translations`-Subtable. Setzt `items`/`regions`/`trust_signals`/`buttons` als alias-o2m auf den Parent-Blocks.

**Files:**
- Modify: `scripts/i18n-setup.mjs`

- [ ] **Step 1: Konfig der Sub-Collections**

```javascript
// Jede Entry: { parent, collection, parentFkField, sortField, scalarFields, translationFields, aliasFieldOnParent }
const ITEM_SUBCOLLECTIONS = [
  {
    parent: 'block_statsBand', collection: 'block_statsBand_items',
    aliasOnParent: 'items',
    scalarFields: [
      { field: 'icon', type: 'string', schema: {}, meta: { interface: 'input' } },
    ],
    translationFields: [
      { field: 'value', type: 'string', schema: {} },
      { field: 'label', type: 'string', schema: {} },
    ],
  },
  {
    parent: 'block_benefits', collection: 'block_benefits_items',
    aliasOnParent: 'items',
    scalarFields: [
      { field: 'icon', type: 'string', schema: {}, meta: { interface: 'input' } },
    ],
    translationFields: [
      { field: 'title',       type: 'string', schema: {} },
      { field: 'description', type: 'text',   schema: {} },
    ],
  },
  {
    parent: 'block_testimonials', collection: 'block_testimonials_items',
    aliasOnParent: 'items',
    scalarFields: [
      { field: 'name',          type: 'string', schema: {} },
      { field: 'initials',      type: 'string', schema: { max_length: 4 } },
      { field: 'tour',          type: 'uuid',   schema: {}, meta: { interface: 'select-dropdown-m2o', special: ['m2o'] }, related: 'touren' },
      { field: 'tour_fallback', type: 'string', schema: {}, meta: { note: 'Fallback-Text falls Tour-Relation null' } },
    ],
    translationFields: [
      { field: 'quote', type: 'text', schema: {} },
    ],
  },
  {
    parent: 'block_regionList', collection: 'block_regionList_regions',
    aliasOnParent: 'regions',
    scalarFields: [
      { field: 'tours', type: 'integer', schema: { default_value: 0 } },
    ],
    translationFields: [
      { field: 'name', type: 'string', schema: {} },
    ],
  },
  {
    parent: 'block_heroBanner', collection: 'block_heroBanner_trust_signals',
    aliasOnParent: 'trust_signals',
    scalarFields: [
      { field: 'icon', type: 'string', schema: {}, meta: { interface: 'input' } },
    ],
    translationFields: [
      { field: 'label', type: 'string', schema: {} },
    ],
  },
  {
    parent: 'block_imageText', collection: 'block_imageText_buttons',
    aliasOnParent: 'buttons',
    scalarFields: [
      { field: 'url', type: 'string', schema: {} },
    ],
    translationFields: [
      { field: 'label', type: 'string', schema: {} },
    ],
  },
  {
    parent: 'block_banner', collection: 'block_banner_buttons',
    aliasOnParent: 'buttons',
    scalarFields: [
      { field: 'url', type: 'string', schema: {} },
    ],
    translationFields: [
      { field: 'label', type: 'string', schema: {} },
    ],
  },
]
```

- [ ] **Step 2: Ensure-Funktion für Item-Sub-Collections**

```javascript
async function ensureItemSubCollection(cfg) {
  const { parent, collection, aliasOnParent, scalarFields, translationFields } = cfg
  const cols = await directus.request(readCollections())
  const parentFields = await directus.request(readFieldsByCollection(parent))
  const parentPk = parentFields.find((f) => f.schema?.is_primary_key)
  const parentPkType = parentPk?.type ?? 'uuid'
  const parentFk = `${parent}_id`

  // Sub-Collection selbst
  if (!cols.find((c) => c.collection === collection)) {
    if (DRY) { console.log(`  (dry) would create ${collection}`); return }
    await directus.request(createCollection({
      collection, meta: { hidden: true, singleton: false, icon: 'list_alt', sort_field: 'sort' }, schema: {},
      fields: [
        { field: 'id', type: 'uuid',
          meta: { hidden: true, interface: 'input', readonly: true, special: ['uuid'] },
          schema: { is_primary_key: true } },
        { field: parentFk, type: parentPkType,
          meta: { hidden: true, interface: 'input', readonly: true }, schema: {} },
        { field: 'sort', type: 'integer',
          meta: { hidden: true, interface: 'input' }, schema: {} },
        ...scalarFields.map((f) => {
          const { related, ...rest } = f
          return rest
        }),
      ],
    }))
    // Parent-FK-Relation (m2o)
    await directus.request(createRelation({
      collection, field: parentFk, related_collection: parent,
      meta: { one_field: aliasOnParent, sort_field: 'sort', one_deselect_action: 'delete' },
      schema: { on_delete: 'CASCADE' },
    }))
    // Eventuelle weitere Relations (z.B. testimonials.tour → touren)
    for (const f of scalarFields) {
      if (f.related) {
        await directus.request(createRelation({
          collection, field: f.field, related_collection: f.related,
          meta: {}, schema: { on_delete: 'SET NULL' },
        }))
      }
    }
    console.log(`  + ${collection}`)
  } else {
    console.log(`  ✓ ${collection}`)
  }

  // Translations-Subtable
  await ensureTranslationsSubtable(collection, { fields: translationFields, hasSlug: false })
}

async function ensureItemSubCollections() {
  for (const cfg of ITEM_SUBCOLLECTIONS) {
    await ensureItemSubCollection(cfg)
  }
}
```

Im `run()`:

```javascript
  console.log('\n→ Item sub-collections')
  await ensureItemSubCollections()
```

- [ ] **Step 3: Lauf**

```bash
cd backup && yarn i18n:setup 2>&1 | tail -30
```

Erwartet: `+ block_statsBand_items`, `+ block_statsBand_items_translations`, … für alle 7 Sub-Collections und ihre Translations.

- [ ] **Step 4: Smoketest**

```bash
curl -s -H "Authorization: Bearer $TOKEN" "$URL/collections?limit=-1" -o /tmp/c.json && node -e "const d=require('/tmp/c.json').data; const subs=d.map(c=>c.collection).filter(n=>n.endsWith('_items')||n.endsWith('_regions')||n.endsWith('_trust_signals')||n.endsWith('_buttons')); console.log(subs.sort().join('\n'))"
```
Erwartet: die 7 Sub-Collections.

- [ ] **Step 5: Commit**

```bash
git add backup/scripts/i18n-setup.mjs
git commit -m "feat(i18n): add 7 item sub-collections with translations for JSON-array blocks"
```

### Task 6: Daten-Migration (DE + EN Entwürfe in Translations + Items)

Verschiebt bestehende DE-Werte aus Haupttabellen-Feldern in `de-DE`-Translation-Records, löscht die Haupttabellen-Spalten (nur nach erfolgreicher Migration), seedet EN-Entwürfe. Migriert JSON-Arrays in die neuen Item-Sub-Collections.

**Files:**
- Create: `scripts/i18n-content-en.mjs` (EN-Entwurfs-Daten-Modul)
- Modify: `scripts/i18n-setup.mjs`

- [ ] **Step 1: EN-Entwürfe in separatem Modul**

Datei [backup/scripts/i18n-content-en.mjs](../../../scripts/i18n-content-en.mjs) anlegen. Sie exportiert Matcher-Funktionen (per `slug`, `title`, `id`, oder feste Werte) und die EN-Übersetzungen.

```javascript
// backup/scripts/i18n-content-en.mjs
// Erstübersetzungs-Entwürfe (EN). Robby reviewt im Directus-Admin.

export const TOUREN_EN = {
  // key: DE-Slug; value: EN-Übersetzung
  'koenigssee-rundweg': {
    slug: 'koenigssee-loop',
    title: 'Königssee Loop',
    subtitle: 'Alpine idyll at the Bavarian fjord',
    intro: 'Guided half-day tour around Lake Königssee with a boat crossing to St. Bartholomä. Ideal for beginners and families.',
    highlights: [
      'Boat to St. Bartholomä',
      'Echo at the cliff face',
      'Views of Watzmann East Face',
      'Flat lakeside path',
    ],
    included: [
      'Certified guide',
      'Boat ticket',
      'Emergency kit',
      'Tea at the halfway point',
    ],
    not_included: [
      'Food and drinks',
      'Travel to/from Schönau',
      'Personal equipment',
    ],
    meeting_point: 'Königssee boat dock, Schönau am Königssee',
    season: 'May – October',
  },
  'drei-zinnen-umrundung': {
    slug: 'tre-cime-loop',
    title: 'Tre Cime Loop',
    subtitle: 'Classic Dolomites circuit',
    intro: 'Full-day hike around the three iconic peaks of Tre Cime di Lavaredo. Moderate difficulty, unforgettable views.',
    highlights: [
      'Three iconic peaks from every angle',
      'Lunch at Rifugio Locatelli',
      'WWI trail remnants',
      'Panorama across Cadore',
    ],
    included: [
      'Certified guide',
      'Topo map',
      'Insurance',
      'Refreshments at huts',
    ],
    not_included: [
      'Lunch at hut',
      'Toll road fee',
      'Personal gear',
    ],
    meeting_point: 'Rifugio Auronzo parking',
    season: 'June – September',
  },
  'watzmann-ueberschreitung': {
    slug: 'watzmann-traverse',
    title: 'Watzmann Traverse',
    subtitle: 'Two-day alpine classic',
    intro: 'Demanding two-day ridge traverse across all three Watzmann summits. Hut overnight. For experienced hikers.',
    highlights: [
      'All three Watzmann summits',
      'Overnight at Watzmannhaus',
      'Sunrise above the Alps',
      'Exposed ridge sections',
    ],
    included: [
      'Certified alpine guide',
      'Hut reservation',
      'Via-ferrata set',
      'Emergency gear',
    ],
    not_included: [
      'Hut meals',
      'Personal alpine gear',
      'Travel to/from trailhead',
    ],
    meeting_point: 'Wimbachbrücke parking',
    season: 'July – September',
  },
}

export const TOUR_TERMINE_EN = {
  // Empty: no current termine have a `hinweis` populated yet. Structure kept for future.
  // key: tour_termin.id; value: { hinweis }
}

export const PAGES_EN = {
  home: { slug: 'home', title: 'Home' },
}

export const SEO_EN = {
  // Key: SEO-id; value: { title, meta_description }. Wird zur Laufzeit per Name/SEO-Titel gemappt.
  // Alle aktuellen SEO-Einträge bekommen eine generische EN-Variante; Redakteur übersetzt sauber.
  DEFAULT: {
    title: 'Alpenpfad — Guided hikes in the Alps',
    meta_description: 'Guided hiking tours in the Alps. Certified guides, small groups, sustainable organization. Since 2014.',
  },
}

export const BLOCK_SCALARS_EN = {
  block_heroBanner: {
    // key: record-id lookup irrelevant — es gibt aktuell 1 aktiven + 1 ungenutzten Record.
    // Alle bekommen die gleiche EN-Übersetzung (Redakteur korrigiert).
    DEFAULT: {
      title: 'Where the sky meets the peaks.',
      eyebrow: 'Season 2026 — bookings open',
      lead: 'Guided hikes through the most beautiful regions of the Alps — from gentle alpine paths to high-altitude tours.',
      cta_primary_label: 'Discover tours',
      cta_secondary_label: 'Open route planner',
    },
  },
  block_tourGrid: {
    DEFAULT: {
      eyebrow: 'Popular tours',
      headline: 'Hikes you will never forget',
      lead: 'A selection of our most popular routes.',
      cta_label: 'View all tours',
    },
  },
  block_benefits: {
    DEFAULT: {
      eyebrow: 'Why us',
      headline: 'Certified. Small. Sustainable.',
      lead: 'We keep groups small, guides local, and organization carbon-balanced.',
    },
  },
  block_regionList: {
    DEFAULT: {
      eyebrow: 'Regions',
      headline: 'From Berchtesgaden to the Bernese Oberland',
      lead: '18 regions across 5 countries.',
      cta_label: 'Browse regions',
    },
  },
  block_testimonials: {
    DEFAULT: {
      eyebrow: 'Testimonials',
      headline: 'Voices from the trail',
    },
  },
  block_newsletter: {
    DEFAULT: {
      eyebrow: 'Stay up to date',
      headline: 'New tours. New seasons. No spam.',
      lead: 'Monthly newsletter with route suggestions, seasonal tips, and early access to new bookings.',
      placeholder: 'you@example.com',
      cta_label: 'Subscribe',
      success_title: 'Almost done.',
      success_text: 'Please confirm the link we just emailed you.',
    },
  },
  block_carousel: {
    DEFAULT: { title: 'Impressions from the trail' },
  },
  block_imageText: {
    DEFAULT: { text: 'More about us — coming soon in English.' },
  },
  block_text: {
    DEFAULT: { content: '<p>English version coming soon.</p>' },
  },
  block_banner: {
    DEFAULT: { title: 'Open positions' },
  },
}

export const BLOCK_ITEMS_EN = {
  // key: <parent>.<json-key-match-string>; value: EN-Übersetzung des einzelnen Items
  block_statsBand_items: {
    // key: DE-label als identifier
    'Geführte Touren im Programm': { value: '240+', label: 'Guided tours on offer' },
    'Alpenregionen in 5 Ländern':   { value: '18',   label: 'Alpine regions in 5 countries' },
    'Wanderer seit 2014 begleitet': { value: '12,000+', label: 'Hikers guided since 2014' },
  },
  block_benefits_items: {
    'Zertifizierte Guides':  { title: 'Certified guides',     description: 'All alpine guides are UIAGM-certified and know their region inside out.' },
    'Kleine Gruppen':        { title: 'Small groups',         description: 'Max. 8 participants per tour — real nature experience, no crowds on narrow ridges.' },
    'Alle Schwierigkeitsgrade': { title: 'Every difficulty', description: 'From casual panorama paths to two-day tours with hut overnight — we match your level.' },
    'Nachhaltig & lokal':    { title: 'Sustainable & local',  description: 'Local hut hosts, public transport to and from the trailhead, CO₂ offset per tour.' },
  },
  block_testimonials_items: {
    'Marianne Berger': { quote: 'We thought we knew the Alps. After three days with Lukas as our guide, we realized we had only scratched the surface.' },
    'Jonas Fellner':   { quote: 'Perfectly organized from the first email to the ride home. Cozy hut, small friendly group — exactly what we wanted.' },
    'Sophia Kraus':    { quote: 'As a beginner, I was nervous. But our guide Anne paced it perfectly. I will come back — next time a little higher.' },
  },
  block_regionList_regions: {
    'Berchtesgadener Land': { name: 'Berchtesgadener Land' },
    'Dolomiten':            { name: 'Dolomites' },
    'Zillertal':            { name: 'Zillertal' },
    'Berner Oberland':      { name: 'Bernese Oberland' },
    'Ötztal':               { name: 'Ötztal' },
    'Karwendel':            { name: 'Karwendel' },
  },
  block_heroBanner_trust_signals: {
    // currently null; stays empty
  },
  block_imageText_buttons: {
    'mehr über uns': { label: 'More about us' },
  },
  block_banner_buttons: {
    'Stellen ansehen': { label: 'View openings' },
  },
}
```

- [ ] **Step 2: Migrationsfunktion skalare Felder**

Im `i18n-setup.mjs`:

```javascript
import { TOUREN_EN, TOUR_TERMINE_EN, PAGES_EN, SEO_EN, BLOCK_SCALARS_EN } from './i18n-content-en.mjs'
import { updateItem } from '@directus/sdk'

async function migrateScalarTranslations() {
  // Für jede Content-Translation: Backup lesen, pro Record DE-Translation erzeugen (mit Altwerten), EN-Translation erzeugen
  const migrations = [
    { parent: 'touren', fields: CONTENT_TRANSLATIONS.touren.fields, enMap: TOUREN_EN, matchBy: 'slug' },
    { parent: 'tour_termine', fields: CONTENT_TRANSLATIONS.tour_termine.fields, enMap: TOUR_TERMINE_EN, matchBy: 'id' },
    { parent: 'pages', fields: CONTENT_TRANSLATIONS.pages.fields, enMap: PAGES_EN, matchBy: 'slug' },
    { parent: 'seo', fields: CONTENT_TRANSLATIONS.seo.fields, enMap: SEO_EN, matchBy: 'DEFAULT' },
    ...Object.entries(BLOCK_TRANSLATIONS).map(([parent, fields]) => ({
      parent, fields, enMap: BLOCK_SCALARS_EN[parent] ?? { DEFAULT: {} }, matchBy: 'DEFAULT',
    })),
  ]
  for (const m of migrations) {
    await migrateOneScalar(m)
  }
}

async function migrateOneScalar({ parent, fields, enMap, matchBy }) {
  const backupFile = path.join(BACKUP_DIR, `${BACKUP_DATE}-i18n-${parent}.json`)
  const records = JSON.parse(await fs.readFile(backupFile, 'utf8'))
  console.log(`  migrating ${parent} (${records.length} records)`)
  for (const rec of records) {
    const parentFk = `${parent}_id`
    const existing = await directus.request(readItemsSdk(`${parent}_translations`, {
      filter: { [parentFk]: { _eq: rec.id } }, limit: -1,
    }))

    // DE-Translation
    if (!existing.find((t) => t.languages_code === 'de-DE')) {
      const dePayload = { [parentFk]: rec.id, languages_code: 'de-DE' }
      for (const f of fields) {
        dePayload[f.field] = rec[f.field] ?? null
      }
      if (!DRY) await directus.request(createItem(`${parent}_translations`, dePayload))
      console.log(`    + DE ${parent}/${rec.id}`)
    }

    // EN-Translation
    if (!existing.find((t) => t.languages_code === 'en-US')) {
      const matchKey = matchBy === 'DEFAULT' ? 'DEFAULT' : rec[matchBy]
      const enData = enMap[matchKey] ?? enMap.DEFAULT ?? null
      if (enData) {
        const enPayload = { [parentFk]: rec.id, languages_code: 'en-US' }
        for (const f of fields) {
          enPayload[f.field] = enData[f.field] ?? null
        }
        if (!DRY) await directus.request(createItem(`${parent}_translations`, enPayload))
        console.log(`    + EN ${parent}/${rec.id}`)
      } else {
        console.log(`    ⚠ no EN draft for ${parent}/${rec.id} (match=${matchKey}) — skipped`)
      }
    }
  }

  // Skalar-Felder aus der Haupttabelle löschen — NUR wenn alle Records eine DE-Translation haben
  const total = await directus.request(readItemsSdk(`${parent}_translations`, {
    filter: { languages_code: { _eq: 'de-DE' } }, limit: -1, fields: [`${parent}_id`],
  }))
  if (total.length >= records.length) {
    for (const f of fields) {
      const parentFields = await directus.request(readFieldsByCollection(parent))
      if (parentFields.find((pf) => pf.field === f.field)) {
        if (DRY) { console.log(`    (dry) would drop ${parent}.${f.field}`); continue }
        await directus.request(deleteField(parent, f.field))
        console.log(`    - dropped ${parent}.${f.field}`)
      }
    }
  } else {
    console.log(`    ⚠ DE coverage ${total.length}/${records.length} — skipping field removal`)
  }
}
```

- [ ] **Step 3: Migrationsfunktion Item-Sub-Collections**

```javascript
import { BLOCK_ITEMS_EN } from './i18n-content-en.mjs'

// pro Item-Sub-Collection: Schlüssel für Mapping auf EN-Entwurf
const ITEM_KEY_SOURCE = {
  block_statsBand_items: 'label',      // DE-label als Key
  block_benefits_items: 'title',
  block_testimonials_items: 'name',
  block_regionList_regions: 'name',
  block_heroBanner_trust_signals: 'label',
  block_imageText_buttons: 'label',
  block_banner_buttons: 'label',
}

// pro Item-Sub-Collection: welches JSON-Feld aus dem Parent-Record ist die Quelle
const ITEM_JSON_FIELD = {
  block_statsBand_items: 'items',
  block_benefits_items: 'items',
  block_testimonials_items: 'items',
  block_regionList_regions: 'regions',
  block_heroBanner_trust_signals: 'trust_signals',
  block_imageText_buttons: 'buttons',
  block_banner_buttons: 'buttons',
}

async function migrateItemSubCollections() {
  for (const cfg of ITEM_SUBCOLLECTIONS) {
    await migrateOneItemSub(cfg)
  }
}

async function migrateOneItemSub(cfg) {
  const { parent, collection, scalarFields, translationFields } = cfg
  const jsonField = ITEM_JSON_FIELD[collection]
  const keyField = ITEM_KEY_SOURCE[collection]
  const backupFile = path.join(BACKUP_DIR, `${BACKUP_DATE}-i18n-${parent}.json`)
  const records = JSON.parse(await fs.readFile(backupFile, 'utf8'))
  const parentFk = `${parent}_id`
  const enMap = BLOCK_ITEMS_EN[collection] ?? {}

  console.log(`  migrating items: ${collection}`)

  for (const rec of records) {
    const itemsArr = rec[jsonField] ?? []
    if (!Array.isArray(itemsArr) || !itemsArr.length) continue

    // Bereits migrierte Items?
    const existingItems = await directus.request(readItemsSdk(collection, {
      filter: { [parentFk]: { _eq: rec.id } }, limit: -1,
    }))
    if (existingItems.length >= itemsArr.length) {
      console.log(`    ✓ ${collection}/${rec.id} (${existingItems.length} items exist)`)
      continue
    }

    let sortCounter = 10
    for (const jsonItem of itemsArr) {
      const scalarPayload = { [parentFk]: rec.id, sort: sortCounter }
      sortCounter += 10

      for (const f of scalarFields) {
        if (f.field === 'tour') {
          // m2o auf touren: Match per Tour-Titel
          const touren = await directus.request(readItemsSdk('touren', {
            filter: { translations: { _and: [{ languages_code: { _eq: 'de-DE' } }, { title: { _eq: jsonItem.tour } }] } },
            limit: 1, fields: ['id'],
          }))
          if (touren.length) {
            scalarPayload.tour = touren[0].id
          } else {
            scalarPayload.tour = null
            scalarPayload.tour_fallback = jsonItem.tour ?? null
          }
        } else if (f.field === 'tour_fallback') {
          // wird oben gesetzt falls nötig
        } else {
          scalarPayload[f.field] = jsonItem[f.field] ?? null
        }
      }

      if (DRY) {
        console.log(`    (dry) would insert item into ${collection}`)
        continue
      }
      const inserted = await directus.request(createItem(collection, scalarPayload))

      // DE-Translation für dieses Item
      const dePayload = { [`${collection}_id`]: inserted.id, languages_code: 'de-DE' }
      for (const tf of translationFields) {
        dePayload[tf.field] = jsonItem[tf.field] ?? null
      }
      await directus.request(createItem(`${collection}_translations`, dePayload))

      // EN-Translation für dieses Item
      const key = jsonItem[keyField]
      const enData = enMap[key]
      if (enData) {
        const enPayload = { [`${collection}_id`]: inserted.id, languages_code: 'en-US' }
        for (const tf of translationFields) {
          enPayload[tf.field] = enData[tf.field] ?? null
        }
        await directus.request(createItem(`${collection}_translations`, enPayload))
      } else {
        console.log(`      ⚠ no EN draft for item key="${key}" in ${collection}`)
      }
    }
    console.log(`    + ${collection}/${rec.id} (${itemsArr.length} items)`)
  }

  // JSON-Feld aus Parent-Tabelle entfernen nach Migration
  const parentFields = await directus.request(readFieldsByCollection(parent))
  if (parentFields.find((f) => f.field === jsonField)) {
    if (DRY) { console.log(`    (dry) would drop ${parent}.${jsonField}`); return }
    await directus.request(deleteField(parent, jsonField))
    console.log(`    - dropped ${parent}.${jsonField}`)
  }
}
```

Im `run()`:

```javascript
  console.log('\n→ Migrate scalar content')
  await migrateScalarTranslations()
  console.log('\n→ Migrate item sub-collections')
  await migrateItemSubCollections()
```

- [ ] **Step 4: Lauf**

```bash
cd backup && yarn i18n:setup 2>&1 | tail -50
```

Erwartet: DE+EN-Translations für alle Content-Records, Item-Records in 7 Sub-Collections, JSON-Spalten aus den Parent-Tables entfernt.

- [ ] **Step 5: Smoketest**

```bash
# DE-Translation einer Tour abrufen
curl -s -H "Authorization: Bearer $TOKEN" "$URL/items/touren_translations?filter[languages_code][_eq]=de-DE&limit=-1" -o /tmp/tde.json && node -e "const d=require('/tmp/tde.json').data; console.log('DE touren:',d.length,d.map(t=>t.slug).join(','))"
# EN-Translation
curl -s -H "Authorization: Bearer $TOKEN" "$URL/items/touren_translations?filter[languages_code][_eq]=en-US&limit=-1" -o /tmp/ten.json && node -e "const d=require('/tmp/ten.json').data; console.log('EN touren:',d.length,d.map(t=>t.slug).join(','))"
# Items
curl -s -H "Authorization: Bearer $TOKEN" "$URL/items/block_benefits_items?limit=-1" -o /tmp/bi.json && node -e "const d=require('/tmp/bi.json').data; console.log('benefits items:',d.length)"
```

Erwartet: 3 DE-touren mit DE-Slugs, 3 EN-touren mit EN-Slugs, 4 benefits-items.

- [ ] **Step 6: Zweiter Lauf = idempotent**

```bash
cd backup && yarn i18n:setup 2>&1 | tail -30
```

Erwartet: Nur `✓`-Zeilen, keine `+ `-Zeilen mehr.

- [ ] **Step 7: Commit**

```bash
git add backup/scripts/i18n-setup.mjs backup/scripts/i18n-content-en.mjs
git commit -m "feat(i18n): migrate DE content + seed EN drafts into translations + item subs"
```

### Task 7: Navigation-Seed + abschließender Konsistenz-Report

Legt die 3 Navigation-Records (`Main`, `Footer`, `FooterLegal`) an, füllt `navigation_items` mit DE + EN Translations. Erweitert die Kunde-Policy um `read`-Permissions auf alle neuen Translations- und Item-Sub-Collections. Gibt am Ende einen Konsistenz-Report aus.

**Files:**
- Modify: `scripts/i18n-setup.mjs`

- [ ] **Step 1: Navigation-Translation-Schema zuerst sicherstellen**

In `ensureContentTranslations` die Navigation-Items als weiteres Target hinzufügen. Einfacher Weg: eine separate `ensureNavigationItemsTranslation()`-Funktion, die die Translations-Subtable für `navigation_items` anlegt:

```javascript
async function ensureNavigationItemsTranslations() {
  await ensureTranslationsSubtable('navigation_items', {
    fields: [
      { field: 'title', type: 'string', schema: {} },
    ],
    hasSlug: false,
  })
}
```

Und im `run()` **vor** `seedNavigation`:

```javascript
  console.log('\n→ Navigation items translations')
  await ensureNavigationItemsTranslations()
```

- [ ] **Step 2: Navigation-Daten-Konstante**

Im Script:

```javascript
const NAV_SEED = {
  Main: {
    items: [
      { title_de: 'Touren',     title_en: 'Tours',        type: 'url',  url: '/#touren' },
      { title_de: 'Regionen',   title_en: 'Regions',      type: 'url',  url: '/#regionen' },
      { title_de: 'Warum wir',  title_en: 'Why Us',       type: 'url',  url: '/#benefits' },
      { title_de: 'Stimmen',    title_en: 'Testimonials', type: 'url',  url: '/#stimmen' },
    ],
  },
  Footer: {
    items: [
      { title_de: 'Touren',      title_en: 'Tours',   type: 'url', url: '#', children: [
        { title_de: 'Tagestouren',      title_en: 'Day Tours',     type: 'url', url: '#' },
        { title_de: 'Mehrtägig',        title_en: 'Multi-Day',     type: 'url', url: '#' },
        { title_de: 'Hochtouren',       title_en: 'Alpine Tours',  type: 'url', url: '#' },
        { title_de: 'Familientouren',   title_en: 'Family Tours',  type: 'url', url: '#' },
        { title_de: 'Winter-Wandern',   title_en: 'Winter Hiking', type: 'url', url: '#' },
      ]},
      { title_de: 'Regionen',    title_en: 'Regions', type: 'url', url: '#', children: [
        { title_de: 'Berchtesgaden',    title_en: 'Berchtesgaden',    type: 'url', url: '#' },
        { title_de: 'Dolomiten',        title_en: 'Dolomites',        type: 'url', url: '#' },
        { title_de: 'Zillertal',        title_en: 'Zillertal',        type: 'url', url: '#' },
        { title_de: 'Berner Oberland',  title_en: 'Bernese Oberland', type: 'url', url: '#' },
        { title_de: 'Alle Regionen',    title_en: 'All Regions',      type: 'url', url: '#' },
      ]},
      { title_de: 'Unternehmen', title_en: 'Company', type: 'url', url: '#', children: [
        { title_de: 'Über uns',        title_en: 'About',       type: 'url', url: '#' },
        { title_de: 'Unsere Guides',   title_en: 'Our Guides',  type: 'url', url: '#' },
        { title_de: 'Partner',         title_en: 'Partners',    type: 'url', url: '#' },
        { title_de: 'Presse',          title_en: 'Press',       type: 'url', url: '#' },
        { title_de: 'Karriere',        title_en: 'Careers',     type: 'url', url: '#' },
      ]},
      { title_de: 'Service',     title_en: 'Service', type: 'url', url: '#', children: [
        { title_de: 'Kontakt',           title_en: 'Contact',    type: 'url', url: '#' },
        { title_de: 'FAQ',               title_en: 'FAQ',        type: 'url', url: '#' },
        { title_de: 'Ausrüstungs-Guide', title_en: 'Gear Guide', type: 'url', url: '#' },
        { title_de: 'Gutscheine',        title_en: 'Vouchers',   type: 'url', url: '#' },
        { title_de: 'Hilfe',             title_en: 'Help',       type: 'url', url: '#' },
      ]},
    ],
  },
  FooterLegal: {
    items: [
      { title_de: 'Impressum',   title_en: 'Imprint',          type: 'url', url: '#' },
      { title_de: 'Datenschutz', title_en: 'Privacy Policy',   type: 'url', url: '#' },
      { title_de: 'AGB',         title_en: 'Terms of Service', type: 'url', url: '#' },
    ],
  },
}
```

- [ ] **Step 3: Seed-Funktion**

```javascript
async function seedNavigation() {
  for (const [navTitle, cfg] of Object.entries(NAV_SEED)) {
    let navId
    const nav = await directus.request(readItemsSdk('navigation', {
      filter: { title: { _eq: navTitle } }, limit: 1,
    }))
    if (nav.length) {
      navId = nav[0].id
      console.log(`  ✓ nav ${navTitle} (id=${navId})`)
    } else {
      if (DRY) { console.log(`  (dry) would create nav ${navTitle}`); continue }
      const created = await directus.request(createItem('navigation', {
        title: navTitle, isLastMenuItemHighlighted: false,
      }))
      navId = created.id
      console.log(`  + nav ${navTitle} (id=${navId})`)
    }

    // Items
    let sort = 10
    for (const item of cfg.items) {
      const itemId = await seedNavItem(navId, null, item, sort)
      sort += 10
      // Children
      if (item.children) {
        let csort = 10
        for (const child of item.children) {
          await seedNavItem(navId, itemId, child, csort)
          csort += 10
        }
      }
    }
  }
}

async function seedNavItem(navigationId, parentId, item, sortValue) {
  const existing = await directus.request(readItemsSdk('navigation_items', {
    filter: {
      navigation: { _eq: navigationId },
      parent: parentId === null ? { _null: true } : { _eq: parentId },
      translations: { _and: [{ languages_code: { _eq: 'de-DE' } }, { title: { _eq: item.title_de } }] },
    },
    limit: 1,
  }))
  if (existing.length) {
    console.log(`    ✓ item ${item.title_de}`)
    return existing[0].id
  }
  if (DRY) { console.log(`    (dry) would insert item ${item.title_de}`); return null }

  const inserted = await directus.request(createItem('navigation_items', {
    navigation: navigationId,
    parent: parentId,
    sort: sortValue,
    type: item.type,
    url: item.url ?? null,
    open_in_new_tab: item.open_in_new_tab ?? false,
  }))
  await directus.request(createItem('navigation_items_translations', {
    navigation_items_id: inserted.id, languages_code: 'de-DE', title: item.title_de,
  }))
  await directus.request(createItem('navigation_items_translations', {
    navigation_items_id: inserted.id, languages_code: 'en-US', title: item.title_en,
  }))
  console.log(`    + item ${item.title_de} / ${item.title_en}`)
  return inserted.id
}
```

Im `run()`:

```javascript
  console.log('\n→ Navigation seed')
  await seedNavigation()
```

- [ ] **Step 4: Permissions auf Translations + Sub-Collections**

```javascript
async function ensureTranslationReadPermissions() {
  const policies = await directus.request(readPolicies({ filter: { name: { _eq: 'Kunde' } }, limit: 1 }))
  if (!policies.length) return
  const policyId = policies[0].id

  const allCollections = [
    ...Object.keys(CONTENT_TRANSLATIONS).map((c) => `${c}_translations`),
    ...Object.keys(BLOCK_TRANSLATIONS).map((c) => `${c}_translations`),
    ...ITEM_SUBCOLLECTIONS.flatMap((cfg) => [cfg.collection, `${cfg.collection}_translations`]),
    'navigation_items_translations',
  ]

  for (const col of allCollections) {
    const existing = await directus.request(readPermissions({
      filter: { policy: { _eq: policyId }, collection: { _eq: col }, action: { _eq: 'read' } },
      limit: 1,
    }))
    if (existing.length) {
      console.log(`    ✓ read on ${col}`)
      continue
    }
    if (DRY) { console.log(`    (dry) would add read on ${col}`); continue }
    await directus.request(createPermission({
      policy: policyId, collection: col, action: 'read', fields: ['*'], permissions: {}, validation: {},
    }))
    console.log(`    + read on ${col}`)
  }
}
```

Im `run()`:

```javascript
  console.log('\n→ Translation permissions')
  await ensureTranslationReadPermissions()
```

- [ ] **Step 5: Konsistenz-Report**

```javascript
async function printReport() {
  console.log('\n--- i18n report ---')
  const languages = await directus.request(readItemsSdk('languages', { limit: -1 }))
  console.log(`  languages:        ${languages.map((l) => l.code).join(', ')}`)

  for (const parent of Object.keys(CONTENT_TRANSLATIONS)) {
    const main = await directus.request(readItemsSdk(parent, { limit: -1, fields: ['id'] }))
    const de = await directus.request(readItemsSdk(`${parent}_translations`, {
      filter: { languages_code: { _eq: 'de-DE' } }, limit: -1, fields: [`${parent}_id`],
    }))
    const en = await directus.request(readItemsSdk(`${parent}_translations`, {
      filter: { languages_code: { _eq: 'en-US' } }, limit: -1, fields: [`${parent}_id`],
    }))
    console.log(`  ${parent.padEnd(20)} records=${main.length}  DE=${de.length}  EN=${en.length}`)
  }
  for (const parent of Object.keys(BLOCK_TRANSLATIONS)) {
    const main = await directus.request(readItemsSdk(parent, { limit: -1, fields: ['id'] }))
    const de = await directus.request(readItemsSdk(`${parent}_translations`, {
      filter: { languages_code: { _eq: 'de-DE' } }, limit: -1, fields: [`${parent}_id`],
    }))
    const en = await directus.request(readItemsSdk(`${parent}_translations`, {
      filter: { languages_code: { _eq: 'en-US' } }, limit: -1, fields: [`${parent}_id`],
    }))
    console.log(`  ${parent.padEnd(20)} records=${main.length}  DE=${de.length}  EN=${en.length}`)
  }
  for (const cfg of ITEM_SUBCOLLECTIONS) {
    const items = await directus.request(readItemsSdk(cfg.collection, { limit: -1, fields: ['id'] }))
    const de = await directus.request(readItemsSdk(`${cfg.collection}_translations`, {
      filter: { languages_code: { _eq: 'de-DE' } }, limit: -1, fields: [`${cfg.collection}_id`],
    }))
    const en = await directus.request(readItemsSdk(`${cfg.collection}_translations`, {
      filter: { languages_code: { _eq: 'en-US' } }, limit: -1, fields: [`${cfg.collection}_id`],
    }))
    console.log(`  ${cfg.collection.padEnd(40)} items=${items.length}  DE=${de.length}  EN=${en.length}`)
  }
}
```

Im `run()` ganz am Ende:

```javascript
  await printReport()
```

- [ ] **Step 6: Lauf + Report prüfen**

```bash
cd backup && yarn i18n:setup 2>&1 | tail -40
```

Erwartet: Report mit Zeilen wie `touren               records=3  DE=3  EN=3`, Navigation-Items erstellt, Permissions gesetzt.

- [ ] **Step 7: Idempotenz-Check**

```bash
cd backup && yarn i18n:setup 2>&1 | grep -c '^\s*+ '
```

Erster Lauf: viele `+`-Einträge. Zweiter Lauf: `0`.

- [ ] **Step 8: Build-Gate**

```bash
cd backup && yarn build 2>&1 | tail -5
```

Erwartet: grün. (Keine Frontend-Änderungen, der Build sollte unverändert laufen. Wenn Directus-SDK-Types bricht: prüfen, ob `translations`-Alias-Feld die Type-Inferenz in `server/api/content/*.get.ts` stört.)

- [ ] **Step 9: Commit**

```bash
git add backup/scripts/i18n-setup.mjs
git commit -m "feat(i18n): seed navigation records + translation permissions + consistency report"
```

## Phase 2 — Server-APIs mit `locale`-Parameter

Die bestehenden Content-APIs akzeptieren jetzt `?locale=de|en` und liefern die passende Translation. Fehlt der Param, default = `de`. Fehlt die Translation, 404. Neue APIs für Navigation + Languages.

### Task 8: Locale-Utility + Homepage-Endpoint

Grundlage für alle weiteren APIs: Mapping-Util, Deep-Filter-Helper, und der erste konkrete Endpoint.

**Files:**
- Create: `server/utils/i18n.ts`
- Modify: `server/api/content/homepage.get.ts`

- [ ] **Step 1: Utility anlegen**

Datei [backup/server/utils/i18n.ts](../../../server/utils/i18n.ts):

```ts
export const SUPPORTED_LOCALES = ['de', 'en'] as const
export type SupportedLocale = typeof SUPPORTED_LOCALES[number]

const NUXT_TO_DIRECTUS: Record<string, string> = { de: 'de-DE', en: 'en-US' }

export function toDirectusLocale(nuxtLocale: unknown): string {
  if (typeof nuxtLocale !== 'string') return 'de-DE'
  return NUXT_TO_DIRECTUS[nuxtLocale] ?? 'de-DE'
}

export function toNuxtLocale(directusLocale: string): SupportedLocale {
  const rev: Record<string, SupportedLocale> = { 'de-DE': 'de', 'en-US': 'en' }
  return rev[directusLocale] ?? 'de'
}
```

- [ ] **Step 2: Bestehenden Homepage-Endpoint lesen**

```bash
cat backup/server/api/content/homepage.get.ts
```

Ziel: die Abfrage auf `general.homepage` (m2o → `pages`) erweitern, sodass die `pages_translations` + `blocks.<item>.translations` mit dem gewünschten Locale kommen.

- [ ] **Step 3: Homepage-Endpoint umbauen**

Überschreibe [backup/server/api/content/homepage.get.ts](../../../server/api/content/homepage.get.ts) mit:

```ts
import { readSingleton, readItem } from '@directus/sdk'
import { directus } from '../../utils/directus'
import { toDirectusLocale } from '../../utils/i18n'

export default defineEventHandler(async (event) => {
  const query = getQuery(event)
  const locale = toDirectusLocale(query.locale)

  const general = await directus.request(readSingleton('general', {
    fields: ['id', 'homepage'],
  }))

  if (!general?.homepage) {
    throw createError({ statusCode: 404, statusMessage: 'Homepage not configured' })
  }

  const page = await directus.request(readItem('pages', general.homepage, {
    fields: [
      'id', 'status', 'seo',
      'translations.*',
      'blocks.*',
      'blocks.item.*',
      'blocks.item.translations.*',
    ],
    deep: {
      translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
      blocks: {
        item: {
          translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
        },
      },
    },
  }))

  const pageT = page.translations?.[0]
  if (!pageT) {
    throw createError({ statusCode: 404, statusMessage: 'Homepage translation missing' })
  }

  // Flatten page
  const { translations, ...pageRest } = page
  return {
    ...pageRest,
    ...pageT,
  }
})
```

- [ ] **Step 4: Dev-Server starten + Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 10
curl -s 'http://localhost:3000/api/content/homepage?locale=de' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('title:',j.title); console.log('blocks:',j.blocks?.length)})"
```
Erwartet: `title: Home`, `blocks: 12`.

```bash
curl -s 'http://localhost:3000/api/content/homepage?locale=en' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('title:',j.title); console.log('blocks:',j.blocks?.length)})"
```
Erwartet: `title: Home` (EN-Translation der Home-Page), gleiche Block-Anzahl.

- [ ] **Step 5: Commit**

```bash
git add backup/server/utils/i18n.ts backup/server/api/content/homepage.get.ts
git commit -m "feat(i18n): add locale util + locale-aware homepage endpoint"
```

### Task 9: Tour-Endpoint mit `alternate_locales`

**Files:**
- Modify: `server/api/content/tour.get.ts`

- [ ] **Step 1: Bestehenden Endpoint lesen**

```bash
cat backup/server/api/content/tour.get.ts
```

- [ ] **Step 2: Endpoint umbauen**

Überschreibe [backup/server/api/content/tour.get.ts](../../../server/api/content/tour.get.ts):

```ts
import { readItems } from '@directus/sdk'
import { directus } from '../../utils/directus'
import { toDirectusLocale } from '../../utils/i18n'

export default defineEventHandler(async (event) => {
  const query = getQuery(event)
  const slug = query.slug as string
  const locale = toDirectusLocale(query.locale)
  if (!slug) throw createError({ statusCode: 400, statusMessage: 'slug required' })

  const rows = await directus.request(readItems('touren', {
    filter: {
      status: { _eq: 'published' },
      translations: {
        _and: [
          { languages_code: { _eq: locale } },
          { slug: { _eq: slug } },
        ],
      },
    },
    fields: [
      '*',
      'gallery.directus_files_id.*',
      'translations.*',
    ],
    deep: {
      translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
    },
    limit: 1,
  }))

  if (!rows.length) {
    throw createError({ statusCode: 404, statusMessage: 'Tour not found in this locale' })
  }
  const tour = rows[0]
  const t = tour.translations?.[0]
  if (!t) throw createError({ statusCode: 404, statusMessage: 'Translation missing' })

  // alternate_locales: slugs pro Sprache, für Language-Switcher
  const altRows = await directus.request(readItems('touren_translations', {
    filter: { touren_id: { _eq: tour.id } },
    fields: ['languages_code', 'slug'],
    limit: -1,
  }))

  // Termine mit hinweis-Translation
  const termine = await directus.request(readItems('tour_termine', {
    filter: {
      status: { _eq: 'published' },
      tour: { _eq: tour.id },
      date_from: { _gte: new Date().toISOString().slice(0, 10) },
    },
    fields: ['*', 'translations.*'],
    deep: {
      translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
    },
    sort: ['sort', 'date_from'],
  }))

  const terminePayload = termine.map((termin) => {
    const tt = termin.translations?.[0] ?? {}
    const { translations: _drop, ...rest } = termin
    return { ...rest, hinweis: tt.hinweis ?? null }
  })

  const { translations, ...rest } = tour
  return {
    ...rest,
    ...t,
    termine: terminePayload,
    alternate_locales: Object.fromEntries(altRows.map((a) => [a.languages_code, a.slug])),
  }
})
```

- [ ] **Step 3: Smoketest DE**

```bash
curl -s 'http://localhost:3000/api/content/tour?slug=koenigssee-rundweg&locale=de' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('title:',j.title); console.log('slug:',j.slug); console.log('alt:',j.alternate_locales); console.log('termine:',j.termine?.length)})"
```
Erwartet: `title: Königssee-Rundweg`, `slug: koenigssee-rundweg`, `alt: { 'de-DE': 'koenigssee-rundweg', 'en-US': 'koenigssee-loop' }`.

- [ ] **Step 4: Smoketest EN**

```bash
curl -s 'http://localhost:3000/api/content/tour?slug=koenigssee-loop&locale=en' -w '\nHTTP %{http_code}\n' | head -20
```
Erwartet: `title: Königssee Loop`, `HTTP 200`.

- [ ] **Step 5: 404-Test**

```bash
curl -s 'http://localhost:3000/api/content/tour?slug=koenigssee-rundweg&locale=en' -w 'HTTP %{http_code}\n' -o /dev/null
```
Erwartet: `HTTP 404` (DE-Slug auf EN angefordert existiert nicht).

- [ ] **Step 6: Commit**

```bash
git add backup/server/api/content/tour.get.ts
git commit -m "feat(i18n): tour endpoint with locale param + alternate_locales + translated termine"
```

### Task 10: Page-Endpoint

**Files:**
- Modify: `server/api/content/page.get.ts`

- [ ] **Step 1: Endpoint umbauen**

Überschreibe [backup/server/api/content/page.get.ts](../../../server/api/content/page.get.ts):

```ts
import { readItems } from '@directus/sdk'
import { directus } from '../../utils/directus'
import { toDirectusLocale } from '../../utils/i18n'

export default defineEventHandler(async (event) => {
  const query = getQuery(event)
  const slug = query.slug as string
  const locale = toDirectusLocale(query.locale)
  if (!slug) throw createError({ statusCode: 400, statusMessage: 'slug required' })

  const rows = await directus.request(readItems('pages', {
    filter: {
      status: { _eq: 'published' },
      translations: {
        _and: [
          { languages_code: { _eq: locale } },
          { slug: { _eq: slug } },
        ],
      },
    },
    fields: [
      '*',
      'translations.*',
      'blocks.*',
    ],
    deep: {
      translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
    },
    limit: 1,
  }))

  if (!rows.length) {
    throw createError({ statusCode: 404, statusMessage: 'Page not found in this locale' })
  }
  const page = rows[0]
  const t = page.translations?.[0]
  if (!t) throw createError({ statusCode: 404, statusMessage: 'Translation missing' })

  const altRows = await directus.request(readItems('pages_translations', {
    filter: { pages_id: { _eq: page.id } },
    fields: ['languages_code', 'slug'],
    limit: -1,
  }))

  const { translations, ...rest } = page
  return {
    ...rest,
    ...t,
    alternate_locales: Object.fromEntries(altRows.map((a) => [a.languages_code, a.slug])),
  }
})
```

- [ ] **Step 2: Smoketest**

```bash
curl -s 'http://localhost:3000/api/content/page?slug=home&locale=de' -w '\nHTTP %{http_code}\n' | head -10
curl -s 'http://localhost:3000/api/content/page?slug=home&locale=en' -w '\nHTTP %{http_code}\n' | head -10
```
Beide Erwartet: `HTTP 200`.

- [ ] **Step 3: Commit**

```bash
git add backup/server/api/content/page.get.ts
git commit -m "feat(i18n): page endpoint with locale param + alternate_locales"
```

### Task 11: Block-Endpoint (inkl. Item-Sub-Collections)

Der umfangreichste API-Endpoint: muss pro Block-Typ einen Deep-Join auf Translations machen, und bei 7 Block-Typen zusätzlich auf die Item-Sub-Collection + deren Translations.

**Files:**
- Modify: `server/api/content/block.get.ts`

- [ ] **Step 1: Bestehenden Endpoint lesen**

```bash
cat backup/server/api/content/block.get.ts
```

Notiere: Whitelist der Block-Typen, deren Feld-Spezifikation.

- [ ] **Step 2: Endpoint-Schema für i18n anpassen**

Überschreibe [backup/server/api/content/block.get.ts](../../../server/api/content/block.get.ts). Der Endpoint wird generisch umgebaut: pro Block-Typ eine Konfig mit `fields` + `hasTranslation` + optional `itemsField`.

```ts
import { readItem } from '@directus/sdk'
import { directus } from '../../utils/directus'
import { toDirectusLocale } from '../../utils/i18n'

type BlockConfig = {
  /** Block-Feld-Selektion (inkl. Relations) */
  fields: string[]
  /** hat eigene Translations-Subtable */
  hasTranslation: boolean
  /** Name des Alias-o2m-Felds auf dem Parent (falls Items vorhanden) */
  itemsAliasField?: string
  /** Sub-Collection-Name für Items (falls vorhanden) */
  itemsCollection?: string
  /** Deep-Filter-Felder der Items (inkl. ihrer Translations) */
  itemsFields?: string[]
}

const BLOCK_CONFIG: Record<string, BlockConfig> = {
  block_heroBanner: {
    fields: ['*', 'image_sky.*', 'image_back.*', 'image_mid.*', 'image_front.*', 'translations.*', 'trust_signals.*', 'trust_signals.translations.*'],
    hasTranslation: true,
    itemsAliasField: 'trust_signals',
    itemsCollection: 'block_heroBanner_trust_signals',
    itemsFields: ['id', 'icon', 'sort', 'translations.label'],
  },
  block_statsBand: {
    fields: ['*', 'items.*', 'items.translations.*'],
    hasTranslation: false,
    itemsAliasField: 'items',
    itemsCollection: 'block_statsBand_items',
    itemsFields: ['id', 'icon', 'sort', 'translations.value', 'translations.label'],
  },
  block_tourGrid: {
    fields: ['*', 'translations.*', 'tours.touren_id.*', 'tours.touren_id.translations.*'],
    hasTranslation: true,
  },
  block_benefits: {
    fields: ['*', 'translations.*', 'items.*', 'items.translations.*'],
    hasTranslation: true,
    itemsAliasField: 'items',
    itemsCollection: 'block_benefits_items',
    itemsFields: ['id', 'icon', 'sort', 'translations.title', 'translations.description'],
  },
  block_regionList: {
    fields: ['*', 'image.*', 'translations.*', 'regions.*', 'regions.translations.*'],
    hasTranslation: true,
    itemsAliasField: 'regions',
    itemsCollection: 'block_regionList_regions',
    itemsFields: ['id', 'tours', 'sort', 'translations.name'],
  },
  block_testimonials: {
    fields: ['*', 'translations.*', 'items.*', 'items.tour.id', 'items.tour.translations.*', 'items.translations.*'],
    hasTranslation: true,
    itemsAliasField: 'items',
    itemsCollection: 'block_testimonials_items',
    itemsFields: ['id', 'name', 'initials', 'sort', 'tour', 'tour_fallback', 'translations.quote'],
  },
  block_newsletter: { fields: ['*', 'translations.*'], hasTranslation: true },
  block_carousel: { fields: ['*', 'images.directus_files_id.*', 'translations.*'], hasTranslation: true },
  block_imageText: {
    fields: ['*', 'image.*', 'translations.*', 'buttons.*', 'buttons.translations.*'],
    hasTranslation: true,
    itemsAliasField: 'buttons',
    itemsCollection: 'block_imageText_buttons',
    itemsFields: ['id', 'url', 'sort', 'translations.label'],
  },
  block_text: { fields: ['*', 'translations.*'], hasTranslation: true },
  block_banner: {
    fields: ['*', 'translations.*', 'buttons.*', 'buttons.translations.*'],
    hasTranslation: true,
    itemsAliasField: 'buttons',
    itemsCollection: 'block_banner_buttons',
    itemsFields: ['id', 'url', 'sort', 'translations.label'],
  },
  block_reception: { fields: ['*'], hasTranslation: false },
}

function flattenTranslation<T extends { translations?: Array<Record<string, any>> }>(obj: T): Record<string, any> {
  const t = obj.translations?.[0] ?? {}
  const { translations: _drop, ...rest } = obj
  return { ...rest, ...t }
}

function flattenItems(items: Array<Record<string, any>> | undefined): Array<Record<string, any>> {
  if (!items) return []
  return items
    .map((i) => flattenTranslation(i))
    .sort((a, b) => (a.sort ?? 0) - (b.sort ?? 0))
}

export default defineEventHandler(async (event) => {
  const query = getQuery(event)
  const collection = query.collection as string
  const id = query.id as string
  const locale = toDirectusLocale(query.locale)

  if (!collection || !BLOCK_CONFIG[collection]) {
    throw createError({ statusCode: 400, statusMessage: 'invalid collection' })
  }
  if (!id) throw createError({ statusCode: 400, statusMessage: 'id required' })

  const cfg = BLOCK_CONFIG[collection]

  const deep: Record<string, any> = {}
  if (cfg.hasTranslation) {
    deep.translations = { _filter: { languages_code: { _eq: locale } }, _limit: 1 }
  }
  if (cfg.itemsAliasField) {
    deep[cfg.itemsAliasField] = {
      translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
    }
  }
  if (collection === 'block_tourGrid') {
    deep.tours = { touren_id: { translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 } } }
  }
  if (collection === 'block_testimonials') {
    deep.items = {
      ...(deep.items ?? {}),
      translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
      tour: { translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 } },
    }
  }

  const block = await directus.request(readItem(collection, id, {
    fields: cfg.fields as any,
    deep,
  }))

  if (!block) throw createError({ statusCode: 404, statusMessage: 'Block not found' })

  if (cfg.hasTranslation && !(block as any).translations?.[0]) {
    throw createError({ statusCode: 404, statusMessage: 'Block translation missing' })
  }

  const flat = cfg.hasTranslation ? flattenTranslation(block as any) : { ...(block as any) }

  if (cfg.itemsAliasField) {
    flat[cfg.itemsAliasField] = flattenItems((block as any)[cfg.itemsAliasField])
  }

  // Testimonials: tour-Relation flachklopfen (translated tour title)
  if (collection === 'block_testimonials' && flat.items) {
    flat.items = flat.items.map((it: any) => {
      const tourTitle = it.tour?.translations?.[0]?.title ?? it.tour_fallback ?? null
      const tourSlug = it.tour?.translations?.[0]?.slug ?? null
      return { ...it, tour: null, tour_title: tourTitle, tour_slug: tourSlug }
    })
  }

  // tourGrid: Touren flachklopfen
  if (collection === 'block_tourGrid' && (flat as any).tours) {
    (flat as any).tours = (flat as any).tours
      .map((t: any) => {
        const tour = t.touren_id
        if (!tour?.translations?.[0]) return null
        const { translations, ...rest } = tour
        return { ...rest, ...translations[0] }
      })
      .filter(Boolean)
  }

  return flat
})
```

- [ ] **Step 3: Smoketest pro Block-Typ**

```bash
# Einen heroBanner-Block holen (ID aus vorhandenen Blocks)
TOKEN=$(grep '^DIRECTUS_TOKEN=' backup/.env | cut -d= -f2)
URL=$(grep '^DIRECTUS_URL=' backup/.env | cut -d= -f2)
BLOCK_ID=$(curl -s -H "Authorization: Bearer $TOKEN" "$URL/items/block_heroBanner?limit=1" | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>console.log(JSON.parse(d).data[0].id))")
curl -s "http://localhost:3000/api/content/block?collection=block_heroBanner&id=$BLOCK_ID&locale=de" | head -c 300
curl -s "http://localhost:3000/api/content/block?collection=block_heroBanner&id=$BLOCK_ID&locale=en" | head -c 300
```
Erwartet: beide `200`, in DE `title` deutsch, in EN `title` englisch.

```bash
# benefits mit items
BLOCK_ID=$(curl -s -H "Authorization: Bearer $TOKEN" "$URL/items/block_benefits?limit=1" | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>console.log(JSON.parse(d).data[0].id))")
curl -s "http://localhost:3000/api/content/block?collection=block_benefits&id=$BLOCK_ID&locale=de" | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('items:',j.items?.length,'first:',j.items?.[0]?.title)})"
```
Erwartet: `items: 4 first: Zertifizierte Guides`.

- [ ] **Step 4: Commit**

```bash
git add backup/server/api/content/block.get.ts
git commit -m "feat(i18n): block endpoint with translation + item sub-collection deep joins"
```

### Task 12: Navigation- + Languages-Endpoints

**Files:**
- Create: `server/api/content/navigation.get.ts`
- Create: `server/api/content/languages.get.ts`

- [ ] **Step 1: Navigation-Endpoint**

Datei [backup/server/api/content/navigation.get.ts](../../../server/api/content/navigation.get.ts):

```ts
import { readItems } from '@directus/sdk'
import { directus } from '../../utils/directus'
import { toDirectusLocale } from '../../utils/i18n'

type NavItem = {
  id: number
  sort: number | null
  type: string
  url: string | null
  page: { translations?: Array<{ slug: string }> } | null
  open_in_new_tab: boolean
  parent: number | null
  title: string
  href: string
  children: NavItem[]
}

export default defineEventHandler(async (event) => {
  const query = getQuery(event)
  const locale = toDirectusLocale(query.locale)

  const items = await directus.request(readItems('navigation_items', {
    limit: -1,
    fields: [
      'id', 'sort', 'type', 'url', 'open_in_new_tab', 'parent', 'navigation',
      'page.id', 'page.translations.slug',
      'translations.title',
    ],
    deep: {
      translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 },
      page: { translations: { _filter: { languages_code: { _eq: locale } }, _limit: 1 } },
    },
    sort: ['sort'],
  }))

  const navs = await directus.request(readItems('navigation', {
    limit: -1, fields: ['id', 'title', 'isLastMenuItemHighlighted'],
  }))
  const navById: Record<number, { title: string; isLastMenuItemHighlighted: boolean }> = {}
  for (const n of navs) navById[n.id as number] = { title: n.title as string, isLastMenuItemHighlighted: !!n.isLastMenuItemHighlighted }

  // Build nav-name → items[] tree
  const byId = new Map<number, any>()
  for (const raw of items) {
    const title = (raw as any).translations?.[0]?.title ?? ''
    const pageSlug = (raw as any).page?.translations?.[0]?.slug ?? null
    const href = raw.type === 'page'
      ? (pageSlug ? `/${pageSlug}` : '#')
      : (raw.url ?? '#')
    byId.set(raw.id as number, {
      id: raw.id, sort: raw.sort, type: raw.type, url: raw.url, page: null,
      open_in_new_tab: raw.open_in_new_tab, parent: raw.parent,
      title, href, navigation: raw.navigation, children: [],
    })
  }

  const byNav: Record<string, NavItem[]> = {}
  for (const item of byId.values()) {
    const navName = navById[item.navigation]?.title
    if (!navName) continue
    if (item.parent) {
      const parent = byId.get(item.parent)
      if (parent) parent.children.push(item)
    } else {
      if (!byNav[navName]) byNav[navName] = []
      byNav[navName].push(item)
    }
  }
  for (const list of Object.values(byNav)) list.sort((a, b) => (a.sort ?? 0) - (b.sort ?? 0))
  for (const list of Object.values(byNav)) {
    for (const p of list) p.children.sort((a, b) => (a.sort ?? 0) - (b.sort ?? 0))
  }

  return byNav
})
```

- [ ] **Step 2: Languages-Endpoint**

Datei [backup/server/api/content/languages.get.ts](../../../server/api/content/languages.get.ts):

```ts
import { readItems } from '@directus/sdk'
import { directus } from '../../utils/directus'

export default defineEventHandler(async () => {
  const rows = await directus.request(readItems('languages', { limit: -1 }))
  return rows
})
```

- [ ] **Step 3: Smoketests**

```bash
curl -s 'http://localhost:3000/api/content/languages' | head -c 200
```
Erwartet: JSON-Array mit `de-DE`, `en-US`.

```bash
curl -s 'http://localhost:3000/api/content/navigation?locale=de' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('keys:',Object.keys(j)); console.log('Main[0]:',j.Main?.[0]?.title)})"
```
Erwartet: `keys: [ 'Main', 'Footer', 'FooterLegal' ]`, `Main[0]: Touren`.

```bash
curl -s 'http://localhost:3000/api/content/navigation?locale=en' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('Main[0]:',j.Main?.[0]?.title); console.log('Footer[0]:',j.Footer?.[0]?.title); console.log('Footer[0].children.length:',j.Footer?.[0]?.children?.length)})"
```
Erwartet: `Main[0]: Tours`, `Footer[0]: Tours`, `Footer[0].children.length: 5`.

- [ ] **Step 4: Build-Gate**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
```
Erwartet: grün.

- [ ] **Step 5: Commit**

```bash
git add backup/server/api/content/navigation.get.ts backup/server/api/content/languages.get.ts
git commit -m "feat(i18n): add navigation + languages endpoints"
```

## Phase 3 — Nuxt i18n Setup + UI-String-Migration

`@nuxtjs/i18n` installieren, konfigurieren, Locale-Files anlegen. Alle hardcoded deutschen Strings in Pages und Komponenten auf `$t()` umstellen. Slug-Translation kommt noch nicht (Phase 4) — vorläufig Route-Pfade wie `/de/anmelden`, `/en/anmelden`.

### Task 13: @nuxtjs/i18n installieren + Grundkonfiguration

**Files:**
- Modify: `package.json`
- Modify: `nuxt.config.ts`
- Create: `app/locales/de.json` (leer, Namespaces-Skelett)
- Create: `app/locales/en.json` (leer, Namespaces-Skelett)

- [ ] **Step 1: Installation**

```bash
cd backup && yarn add @nuxtjs/i18n@^9
```

- [ ] **Step 2: `nuxt.config.ts` erweitern**

Bestehenden Inhalt lesen:

```bash
cat backup/nuxt.config.ts
```

`modules`-Array um `'@nuxtjs/i18n'` erweitern, am Ende der Config-Object eine `i18n`-Section hinzufügen:

```ts
// backup/nuxt.config.ts — Ergänzung innerhalb defineNuxtConfig({...})

  modules: [
    // bestehende Module...
    '@nuxtjs/i18n',
  ],

  i18n: {
    strategy: 'prefix',
    defaultLocale: 'de',
    detectBrowserLanguage: {
      useCookie: true,
      cookieKey: 'i18n_redirected',
      cookieSecure: true,
      redirectOn: 'root',
      fallbackLocale: 'de',
    },
    locales: [
      { code: 'de', iso: 'de-DE', name: 'Deutsch', file: 'de.json' },
      { code: 'en', iso: 'en-US', name: 'English', file: 'en.json' },
    ],
    lazy: true,
    langDir: 'locales/',
    baseUrl: process.env.NUXT_PUBLIC_SITE_URL ?? 'https://alpenpfad.example.com',
  },
```

- [ ] **Step 3: Leere Locale-Files mit Namespaces**

Datei [backup/app/locales/de.json](../../../app/locales/de.json):

```json
{
  "common": {},
  "nav": {},
  "cta": {},
  "auth": {},
  "booking": { "status": {} },
  "tour": {},
  "form": {},
  "validation": {},
  "footer": {},
  "language": {}
}
```

[backup/app/locales/en.json](../../../app/locales/en.json): **identisches Skelett**, alle Werte später eingetragen in Task 14.

```json
{
  "common": {},
  "nav": {},
  "cta": {},
  "auth": {},
  "booking": { "status": {} },
  "tour": {},
  "form": {},
  "validation": {},
  "footer": {},
  "language": {}
}
```

- [ ] **Step 4: Dev-Server-Test**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
curl -s -o /dev/null -w "HTTP %{http_code} Location %{redirect_url}\n" http://localhost:3000/
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:3000/de/
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:3000/en/
```
Erwartet: `/` → Redirect nach `/de/`; beide Sprachen antworten `200`.

- [ ] **Step 5: Build-Gate**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
```
Erwartet: grün.

- [ ] **Step 6: Commit**

```bash
git add backup/package.json backup/yarn.lock backup/nuxt.config.ts backup/app/locales/de.json backup/app/locales/en.json
git commit -m "feat(i18n): install @nuxtjs/i18n with DE/EN locales and empty skeleton files"
```

### Task 14: Locale-Files mit allen UI-Strings füllen

Alle UI-Strings aus der Spec-Sektion 5.2 in die beiden JSON-Files. Englische Übersetzung liefere ich hier mit.

**Files:**
- Modify: `app/locales/de.json`
- Modify: `app/locales/en.json`

- [ ] **Step 1: `de.json` komplett schreiben**

Überschreibe [backup/app/locales/de.json](../../../app/locales/de.json):

```json
{
  "common": {
    "home": "Startseite",
    "back": "Zurück",
    "loading": "Lädt…",
    "error": "Ein Fehler ist aufgetreten.",
    "not_found_title": "Seite nicht gefunden",
    "not_found_lead": "Diese Seite existiert nicht oder ist nicht verfügbar.",
    "not_available_in_language": "Nicht in dieser Sprache verfügbar",
    "empty_list": "Keine Inhalte verfügbar."
  },
  "nav": {
    "toggle_open": "Menü öffnen",
    "toggle_close": "Menü schließen",
    "site_title": "Alpenpfad",
    "main_navigation": "Hauptnavigation"
  },
  "cta": {
    "book_tour": "Tour buchen",
    "discover_tours": "Touren entdecken",
    "contact_us": "Schreib uns",
    "learn_more": "Mehr erfahren"
  },
  "auth": {
    "login": "Anmelden",
    "logout": "Abmelden",
    "register": "Registrieren",
    "forgot_password": "Passwort vergessen?",
    "email": "E-Mail",
    "password": "Passwort",
    "password_repeat": "Passwort wiederholen",
    "login_cta": "Anmelden",
    "login_sub": "Bei deinem Alpenpfad-Konto",
    "no_account_yet": "Noch keinen Account?",
    "already_registered": "Schon registriert?",
    "create_account": "Account anlegen",
    "reset_password": "Passwort zurücksetzen",
    "reset_request": "Link zum Zurücksetzen anfordern",
    "reset_request_sent": "Falls die E-Mail-Adresse bei uns registriert ist, haben wir dir einen Reset-Link geschickt.",
    "account": "Mein Konto",
    "profile": "Profil",
    "profile_lead": "Pflege hier dein Profil",
    "security": "Sicherheit",
    "first_name": "Vorname",
    "last_name": "Nachname",
    "save_profile": "Profil speichern",
    "change_password": "Passwort ändern",
    "current_password": "Aktuelles Passwort",
    "new_password": "Neues Passwort",
    "avatar": "Profilbild",
    "avatar_upload": "Bild hochladen",
    "avatar_remove": "Bild entfernen",
    "register_sub": "Account in 10 Sekunden anlegen",
    "account_created": "Account angelegt. Du bist eingeloggt."
  },
  "booking": {
    "book_now": "Jetzt buchen",
    "book_soon": "Buchung in Kürze",
    "sold_out": "Ausgebucht",
    "places_left": "noch {count} Plätze",
    "select_date": "Wähle einen Termin oder gib ein Wunschdatum an",
    "select_placeholder": "Bitte wählen…",
    "wish_date": "Wunschdatum",
    "wish_date_placeholder": "Wunschdatum angeben…",
    "contact_data": "Kontaktdaten",
    "first_name": "Vorname",
    "last_name": "Nachname",
    "email": "E-Mail",
    "phone": "Telefon",
    "persons": "Personen",
    "notes": "Besondere Wünsche",
    "submit_booking": "Buchung anfragen",
    "my_bookings": "Meine Buchungen",
    "booking_detail": "Buchungs-Detail",
    "cancel_booking": "Buchung stornieren",
    "cancel_confirm": "Möchtest du diese Buchung wirklich stornieren?",
    "empty_list": "Du hast noch keine Buchungen.",
    "price_total": "Gesamtpreis",
    "status": {
      "angefragt": "Angefragt",
      "bestaetigt": "Bestätigt",
      "storniert": "Storniert",
      "abgelehnt": "Abgelehnt",
      "durchgefuehrt": "Durchgeführt"
    }
  },
  "tour": {
    "duration": "Dauer",
    "difficulty": "Schwierigkeit",
    "distance": "Länge",
    "ascent": "Höhenmeter",
    "included": "Im Preis enthalten",
    "not_included": "Nicht im Preis enthalten",
    "meeting_point": "Treffpunkt",
    "season": "Saison",
    "organizational": "Organisatorisches",
    "highlights": "Highlights",
    "next_dates": "Nächste Termine",
    "group_size_max": "Gruppe max. {count}",
    "price_from": "ab {price} €",
    "per_person": "pro Person"
  },
  "form": {
    "required": "Pflichtfeld",
    "submit": "Absenden",
    "cancel": "Abbrechen",
    "save": "Speichern",
    "loading": "Wird gesendet…",
    "success": "Gespeichert."
  },
  "validation": {
    "required": "Dieses Feld ist erforderlich.",
    "email": "Bitte eine gültige E-Mail-Adresse angeben.",
    "min_length": "Mindestens {count} Zeichen.",
    "max_length": "Maximal {count} Zeichen.",
    "passwords_dont_match": "Passwörter stimmen nicht überein.",
    "min_persons": "Mindestens 1 Person.",
    "invalid": "Ungültig."
  },
  "footer": {
    "brand_claim": "Seit 2014 geführte Wanderungen in den Alpen. Zertifizierte Guides, kleine Gruppen, nachhaltige Organisation.",
    "copyright": "© {year} Alpenpfad GmbH. Alle Rechte vorbehalten.",
    "social_aria": "Social Media"
  },
  "language": {
    "switch_to": "Auf {name} wechseln"
  }
}
```

- [ ] **Step 2: `en.json` komplett schreiben**

Überschreibe [backup/app/locales/en.json](../../../app/locales/en.json):

```json
{
  "common": {
    "home": "Home",
    "back": "Back",
    "loading": "Loading…",
    "error": "Something went wrong.",
    "not_found_title": "Page not found",
    "not_found_lead": "This page doesn't exist or isn't available.",
    "not_available_in_language": "Not available in this language",
    "empty_list": "No content available."
  },
  "nav": {
    "toggle_open": "Open menu",
    "toggle_close": "Close menu",
    "site_title": "Alpenpfad",
    "main_navigation": "Main navigation"
  },
  "cta": {
    "book_tour": "Book a tour",
    "discover_tours": "Discover tours",
    "contact_us": "Contact us",
    "learn_more": "Learn more"
  },
  "auth": {
    "login": "Log in",
    "logout": "Log out",
    "register": "Sign up",
    "forgot_password": "Forgot password?",
    "email": "Email",
    "password": "Password",
    "password_repeat": "Repeat password",
    "login_cta": "Log in",
    "login_sub": "To your Alpenpfad account",
    "no_account_yet": "No account yet?",
    "already_registered": "Already registered?",
    "create_account": "Create account",
    "reset_password": "Reset password",
    "reset_request": "Request reset link",
    "reset_request_sent": "If the email is registered with us, we've sent you a reset link.",
    "account": "My account",
    "profile": "Profile",
    "profile_lead": "Manage your profile here",
    "security": "Security",
    "first_name": "First name",
    "last_name": "Last name",
    "save_profile": "Save profile",
    "change_password": "Change password",
    "current_password": "Current password",
    "new_password": "New password",
    "avatar": "Avatar",
    "avatar_upload": "Upload image",
    "avatar_remove": "Remove image",
    "register_sub": "Create an account in 10 seconds",
    "account_created": "Account created. You are logged in."
  },
  "booking": {
    "book_now": "Book now",
    "book_soon": "Booking opening soon",
    "sold_out": "Sold out",
    "places_left": "{count} spots left",
    "select_date": "Pick a date or enter your preferred date",
    "select_placeholder": "Please choose…",
    "wish_date": "Preferred date",
    "wish_date_placeholder": "Enter preferred date…",
    "contact_data": "Contact details",
    "first_name": "First name",
    "last_name": "Last name",
    "email": "Email",
    "phone": "Phone",
    "persons": "Participants",
    "notes": "Special requests",
    "submit_booking": "Request booking",
    "my_bookings": "My bookings",
    "booking_detail": "Booking detail",
    "cancel_booking": "Cancel booking",
    "cancel_confirm": "Are you sure you want to cancel this booking?",
    "empty_list": "You have no bookings yet.",
    "price_total": "Total price",
    "status": {
      "angefragt": "Requested",
      "bestaetigt": "Confirmed",
      "storniert": "Cancelled",
      "abgelehnt": "Declined",
      "durchgefuehrt": "Completed"
    }
  },
  "tour": {
    "duration": "Duration",
    "difficulty": "Difficulty",
    "distance": "Distance",
    "ascent": "Elevation",
    "included": "Included",
    "not_included": "Not included",
    "meeting_point": "Meeting point",
    "season": "Season",
    "organizational": "Organizational",
    "highlights": "Highlights",
    "next_dates": "Upcoming dates",
    "group_size_max": "Group max. {count}",
    "price_from": "from {price} €",
    "per_person": "per person"
  },
  "form": {
    "required": "Required",
    "submit": "Submit",
    "cancel": "Cancel",
    "save": "Save",
    "loading": "Submitting…",
    "success": "Saved."
  },
  "validation": {
    "required": "This field is required.",
    "email": "Please enter a valid email address.",
    "min_length": "At least {count} characters.",
    "max_length": "At most {count} characters.",
    "passwords_dont_match": "Passwords do not match.",
    "min_persons": "At least 1 participant.",
    "invalid": "Invalid."
  },
  "footer": {
    "brand_claim": "Guided hikes in the Alps since 2014. Certified guides, small groups, sustainable organization.",
    "copyright": "© {year} Alpenpfad GmbH. All rights reserved.",
    "social_aria": "Social media"
  },
  "language": {
    "switch_to": "Switch to {name}"
  }
}
```

- [ ] **Step 3: Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
curl -s http://localhost:3000/de/ -o /dev/null -w "HTTP %{http_code}\n"
curl -s http://localhost:3000/en/ -o /dev/null -w "HTTP %{http_code}\n"
```
Erwartet: beide `200`. Rendering ist noch überwiegend deutsch — das ändert sich erst in Tasks 15–19.

- [ ] **Step 4: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/app/locales/de.json backup/app/locales/en.json
git commit -m "feat(i18n): populate DE/EN locale files with all UI strings"
```

### Task 15: Sections-Komponenten auf `$t()`

Die 7 `SectionXxx.vue`-Komponenten unter [app/components/sections/](../../../app/components/sections/) haben hardcoded deutsche Strings. Ersetzen durch `$t('key')`. Statische Items (Stats, Benefits, Regions, Testimonials) wandern zukünftig nach Directus — in dieser Phase nutzen wir dafür Props mit Fallback auf Locale-Texte, die Phase-5-Integration in Directus-Daten erfolgt in Task 22–25.

**Files:**
- Modify: `app/components/sections/SectionHero.vue`
- Modify: `app/components/sections/SectionStats.vue`
- Modify: `app/components/sections/SectionTours.vue`
- Modify: `app/components/sections/SectionBenefits.vue`
- Modify: `app/components/sections/SectionRegions.vue`
- Modify: `app/components/sections/SectionTestimonials.vue`
- Modify: `app/components/sections/SectionNewsletter.vue`

- [ ] **Step 1: Vorlage am Beispiel `SectionTours.vue`**

Bestehenden Inhalt lesen:

```bash
cat backup/app/components/sections/SectionTours.vue
```

Pattern: jeden hardcoded deutschen String finden und durch `{{ $t('key') }}` ersetzen. Falls in `<script setup>`: `const { t } = useI18n()` und `t('key')`.

Beispiel (adaptiere Keys zum tatsächlichen Inhalt):

```vue
<!-- Vorher -->
<h2>Beliebte Touren</h2>
<p class="lead">Wanderungen, die man nicht vergisst</p>
<a href="#">Touren entdecken</a>

<!-- Nachher -->
<h2>{{ $t('cta.discover_tours') }}</h2>
<!-- Spezifischerer Key wenn möglich — hier stellvertretend cta.discover_tours -->
```

Für die Strings der SectionHero/Stats/Benefits etc., die aus Directus kommen sollen: **Props mit Fallback**. Die Komponente erhält `:eyebrow`, `:headline`, `:lead` von der Page (die sie aus der API lädt) und nutzt `$t()` nur für echte UI-Labels (Buttons wie "Discover tours").

- [ ] **Step 2: SectionHero.vue**

Finde hardcoded Strings (z.B. "Dort, wo der Himmel die Gipfel berührt.", "Saison 2026 — jetzt buchbar"). Diese Texte kommen aus Directus (`block_heroBanner_translations`), werden also **nicht** ins Locale-File verschoben. Die Komponente nimmt sie als Props entgegen.

```vue
<!-- backup/app/components/sections/SectionHero.vue — Beispiel-Umbau -->
<script setup lang="ts">
defineProps<{
  eyebrow?: string
  title?: string
  lead?: string
  ctaPrimaryLabel?: string
  ctaPrimaryHref?: string
  ctaSecondaryLabel?: string
  ctaSecondaryHref?: string
}>()
</script>

<template>
  <section class="relative">
    <!-- Illustration -->
    <!-- ... SVG-Bergpanorama bleibt unverändert ... -->

    <div class="content">
      <span v-if="eyebrow" class="eyebrow">{{ eyebrow }}</span>
      <h1 v-if="title">{{ title }}</h1>
      <p v-if="lead">{{ lead }}</p>
      <div class="ctas">
        <Button v-if="ctaPrimaryLabel" as="a" :href="ctaPrimaryHref ?? '#'">{{ ctaPrimaryLabel }}</Button>
        <Button v-if="ctaSecondaryLabel" variant="ghost" as="a" :href="ctaSecondaryHref ?? '#'">{{ ctaSecondaryLabel }}</Button>
      </div>
    </div>
  </section>
</template>
```

- [ ] **Step 3: SectionStats.vue**

Komponente erhält `items` als Prop (bisher hardcoded). In Task 22–25 wird die Home-Page diese Items aus `block_statsBand_items` über die API laden und an die Section durchreichen.

```vue
<!-- backup/app/components/sections/SectionStats.vue -->
<script setup lang="ts">
defineProps<{
  items: Array<{ icon: string; value: string; label: string }>
}>()
</script>

<template>
  <section>
    <div class="grid">
      <div v-for="it in items" :key="it.label" class="stat">
        <span class="value">{{ it.value }}</span>
        <span class="label">{{ it.label }}</span>
      </div>
    </div>
  </section>
</template>
```

- [ ] **Step 4: SectionTours.vue, SectionBenefits.vue, SectionRegions.vue, SectionTestimonials.vue**

Gleiches Muster. Jede Section erhält `eyebrow`, `headline`, `lead`, optional `ctaLabel` als Props; `items`/`regions`/`testimonials` als Liste-Props mit bereits übersetzten Werten.

Für `SectionTours`:
```vue
<script setup lang="ts">
defineProps<{
  eyebrow?: string
  headline?: string
  lead?: string
  ctaLabel?: string
  ctaHref?: string
  tours: Array<{ id: string; slug: string; title: string; region?: string; difficulty?: string; /* ... */ }>
}>()
</script>
```

Für `SectionTestimonials`:
```vue
<script setup lang="ts">
defineProps<{
  eyebrow?: string
  headline?: string
  items: Array<{ name: string; initials: string; tour_title: string | null; quote: string }>
}>()
</script>
```

- [ ] **Step 5: SectionNewsletter.vue**

Die Section nutzt sowohl Directus-Content (Headline, Placeholder) als auch UI-Strings (z.B. Button-State „Wird gesendet…").

```vue
<script setup lang="ts">
const { t } = useI18n()
defineProps<{
  eyebrow?: string
  headline?: string
  lead?: string
  placeholder?: string
  ctaLabel?: string
  successTitle?: string
  successText?: string
}>()

const email = ref('')
const loading = ref(false)
const done = ref(false)

async function submit() {
  loading.value = true
  // existing submit logic unchanged
  loading.value = false
  done.value = true
}
</script>

<template>
  <section>
    <span v-if="eyebrow" class="eyebrow">{{ eyebrow }}</span>
    <h2 v-if="headline">{{ headline }}</h2>
    <p v-if="lead">{{ lead }}</p>
    <form v-if="!done" @submit.prevent="submit">
      <input v-model="email" type="email" :placeholder="placeholder ?? ''" />
      <Button type="submit" :disabled="loading">
        {{ loading ? t('form.loading') : (ctaLabel ?? t('cta.learn_more')) }}
      </Button>
    </form>
    <div v-else>
      <h3>{{ successTitle }}</h3>
      <p>{{ successText }}</p>
    </div>
  </section>
</template>
```

- [ ] **Step 6: Build-Gate**

```bash
cd backup && yarn build 2>&1 | tail -5
```
Erwartet: grün. Falls Fehler: hardcoded Strings vergessen, oder Prop-Typen nicht passend.

- [ ] **Step 7: Commit**

```bash
git add backup/app/components/sections/
git commit -m "feat(i18n): refactor section components to accept translated content as props"
```

### Task 16: Auth-Komponenten auf `$t()`

**Files:**
- Modify: `app/components/Auth/LoginForm.vue`
- Modify: `app/components/Auth/RegisterForm.vue`
- Modify: `app/components/Auth/PasswordResetForm.vue`
- Modify: `app/components/Auth/PasswordResetRequestForm.vue`
- Modify: `app/components/Auth/ProfileForm.vue`
- Modify: `app/components/Auth/UserMenu.vue`
- Modify: `app/components/Auth/UserAvatar.vue`

- [ ] **Step 1: Pattern für alle Auth-Komponenten**

`<script setup>` am Anfang (falls nicht schon da):

```ts
const { t } = useI18n()
```

Im Template alle Label/Placeholder/Button-Strings durch `$t('auth.xxx')` oder `$t('form.xxx')` ersetzen:

```vue
<!-- LoginForm.vue: Vorher -->
<Label>E-Mail</Label>
<Input type="email" />
<Label>Passwort</Label>
<Input type="password" />
<Button type="submit">Anmelden</Button>
<a href="/passwort-vergessen">Passwort vergessen?</a>

<!-- Nachher -->
<Label>{{ $t('auth.email') }}</Label>
<Input type="email" />
<Label>{{ $t('auth.password') }}</Label>
<Input type="password" />
<Button type="submit">{{ $t('auth.login_cta') }}</Button>
<NuxtLink :to="localePath('/passwort-vergessen')">{{ $t('auth.forgot_password') }}</NuxtLink>
```

**Wichtig:** Alle internen Links nutzen ab jetzt `localePath()` (Composable, automatisch verfügbar via `@nuxtjs/i18n`), damit sie das Prefix der aktiven Sprache bekommen.

Siehe: `const localePath = useLocalePath()` in `<script setup>`, oder direkt `$localePath('/…')` im Template.

- [ ] **Step 2: Schema-Fehlermeldungen vorläufig auf Keys umstellen**

Die Zod-Integration mit i18n-Map kommt in Task 26. **Jetzt** belassen wir die Zod-Messages zwischenzeitlich so wie sie sind — das Formular funktioniert weiter auf Deutsch, und Task 26 stellt um.

- [ ] **Step 3: ProfileForm.vue**

Zusätzlich zu den auth-Strings gibt es Avatar-Upload-Strings:

```vue
<Label>{{ $t('auth.avatar') }}</Label>
<Button @click="upload">{{ $t('auth.avatar_upload') }}</Button>
<Button variant="destructive" @click="remove">{{ $t('auth.avatar_remove') }}</Button>
```

- [ ] **Step 4: UserMenu.vue**

```vue
<DropdownMenuItem @click="navigate('/konto')">{{ $t('auth.account') }}</DropdownMenuItem>
<DropdownMenuItem @click="logoutAndGoHome">{{ $t('auth.logout') }}</DropdownMenuItem>
```

- [ ] **Step 5: UserAvatar.vue**

```vue
<span class="sr-only">{{ $t('auth.avatar') }}</span>
<!-- Alt-Text oder aria-label, falls vorhanden -->
```

- [ ] **Step 6: Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
curl -s http://localhost:3000/de/anmelden | grep -o 'Anmelden\|Log in\|E-Mail\|Email\|Passwort\|Password' | sort -u
curl -s http://localhost:3000/en/anmelden | grep -o 'Anmelden\|Log in\|E-Mail\|Email\|Passwort\|Password' | sort -u
```
Erwartet: `/de/` zeigt deutsche Strings, `/en/` englische.

- [ ] **Step 7: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/app/components/Auth/
git commit -m "feat(i18n): localize all Auth components"
```

### Task 17: Konto-Pages auf `$t()`

**Files:**
- Modify: `app/pages/konto/index.vue`
- Modify: `app/pages/konto/passwort.vue`
- Modify: `app/pages/konto/buchungen/index.vue`
- Modify: `app/pages/konto/buchungen/[id].vue`

- [ ] **Step 1: Muster in `konto/index.vue`**

Die Konto-Startseite enthält Strings wie „Mein Konto", „Profil", „Sicherheit", „Pflege hier dein Profil" — alle im `auth`-Namespace:

```vue
<script setup lang="ts">
definePageMeta({ middleware: 'auth' })
const { t } = useI18n()
useSeoMeta({ title: () => `${t('auth.account')} | Alpenpfad` })
</script>

<template>
  <NuxtLayout>
    <h1>{{ $t('auth.account') }}</h1>
    <p>{{ $t('auth.profile_lead') }}</p>

    <Tabs>
      <TabsList>
        <TabsTrigger value="profile">{{ $t('auth.profile') }}</TabsTrigger>
        <TabsTrigger value="security">{{ $t('auth.security') }}</TabsTrigger>
      </TabsList>
      <TabsContent value="profile">
        <AuthProfileForm />
      </TabsContent>
      <TabsContent value="security">
        <NuxtLink :to="localePath('/konto/passwort')">{{ $t('auth.change_password') }}</NuxtLink>
      </TabsContent>
    </Tabs>
  </NuxtLayout>
</template>
```

- [ ] **Step 2: `konto/passwort.vue`**

```vue
<script setup lang="ts">
definePageMeta({ middleware: 'auth' })
const { t } = useI18n()
useSeoMeta({ title: () => `${t('auth.change_password')} | Alpenpfad` })
</script>

<template>
  <NuxtLayout>
    <h1>{{ $t('auth.change_password') }}</h1>
    <AuthPasswordResetForm />  <!-- oder die spezifische change-password-Komponente -->
  </NuxtLayout>
</template>
```

- [ ] **Step 3: `konto/buchungen/index.vue`**

```vue
<script setup lang="ts">
definePageMeta({ middleware: 'auth' })
const { t, locale } = useI18n()
useSeoMeta({ title: () => `${t('booking.my_bookings')} | Alpenpfad` })

const { data: bookings } = await useFetch('/api/buchungen', { query: { locale } })
</script>

<template>
  <NuxtLayout>
    <h1>{{ $t('booking.my_bookings') }}</h1>
    <div v-if="!bookings?.length">{{ $t('booking.empty_list') }}</div>
    <div v-else>
      <BuchungCard v-for="b in bookings" :key="b.id" :booking="b" />
    </div>
  </NuxtLayout>
</template>
```

- [ ] **Step 4: `konto/buchungen/[id].vue`**

```vue
<script setup lang="ts">
definePageMeta({ middleware: 'auth' })
const { t, locale } = useI18n()
const route = useRoute()

const { data: booking } = await useFetch(() => `/api/buchungen/${route.params.id}`, { query: { locale } })
useSeoMeta({ title: () => `${t('booking.booking_detail')} | Alpenpfad` })
</script>

<template>
  <NuxtLayout>
    <h1>{{ $t('booking.booking_detail') }}</h1>
    <BuchungDetail v-if="booking" :booking="booking" />
  </NuxtLayout>
</template>
```

- [ ] **Step 5: Build-Gate + Commit**

```bash
cd backup && yarn build 2>&1 | tail -5
git add backup/app/pages/konto/
git commit -m "feat(i18n): localize konto pages"
```

### Task 18: Tour- und Buchung-Komponenten auf `$t()`

**Files:**
- Modify: `app/components/Tour/Hero.vue`
- Modify: `app/components/Tour/CTA.vue`
- Modify: `app/components/Tour/FactsBar.vue`
- Modify: `app/components/Tour/Highlights.vue`
- Modify: `app/components/Tour/Included.vue`
- Modify: `app/components/Tour/Organizational.vue`
- Modify: `app/components/Tour/Gallery.vue`
- Modify: `app/components/Tour/StickyMobileCTA.vue`
- Modify: `app/components/Tour/TourTermine.vue`
- Modify: `app/components/Buchung/BuchungForm.vue`
- Modify: `app/components/Buchung/BuchungCard.vue`
- Modify: `app/components/Buchung/BuchungDetail.vue`
- Modify: `app/components/Buchung/BuchungStatusBadge.vue`

- [ ] **Step 1: Tour-Komponenten umbauen**

Sechs Tour-Komponenten haben hardcoded Labels wie „Schwierigkeit", „Länge", „Höhenmeter", „Highlights", „Im Preis enthalten", „Treffpunkt", „Saison", „Jetzt buchen", „Buchung in Kürze":

Beispiel [Tour/FactsBar.vue](../../../app/components/Tour/FactsBar.vue):

```vue
<script setup lang="ts">
defineProps<{
  duration?: string
  difficulty?: string
  distance?: string
  ascent?: string
  groupSizeMax?: number
  priceFrom?: number
}>()
</script>

<template>
  <div class="facts">
    <div><span>{{ $t('tour.duration') }}</span><strong>{{ duration }}</strong></div>
    <div><span>{{ $t('tour.difficulty') }}</span><strong>{{ difficulty }}</strong></div>
    <div><span>{{ $t('tour.distance') }}</span><strong>{{ distance }}</strong></div>
    <div><span>{{ $t('tour.ascent') }}</span><strong>{{ ascent }}</strong></div>
    <div><span>{{ $t('tour.group_size_max', { count: groupSizeMax }) }}</span></div>
    <div><span>{{ $t('tour.price_from', { price: priceFrom }) }}</span><small>{{ $t('tour.per_person') }}</small></div>
  </div>
</template>
```

Für `Tour/CTA.vue`, `StickyMobileCTA.vue`:

```vue
<script setup lang="ts">
defineProps<{ bookingUrl?: string | null }>()
const localePath = useLocalePath()
</script>

<template>
  <Button v-if="bookingUrl" as="a" :href="bookingUrl">{{ $t('booking.book_now') }}</Button>
  <Button v-else disabled>{{ $t('booking.book_soon') }}</Button>
</template>
```

Für `Tour/Highlights.vue`, `Included.vue`:

```vue
<script setup lang="ts">
defineProps<{ items: string[] }>()
</script>

<template>
  <section>
    <h3>{{ $t('tour.highlights') }}</h3>
    <ul><li v-for="i in items" :key="i">{{ i }}</li></ul>
  </section>
</template>
```

Für `Tour/TourTermine.vue`:

```vue
<script setup lang="ts">
defineProps<{ termine: Array<{ id: string; date_from: string; date_to: string; hinweis: string | null; verfuegbare_plaetze?: number }> }>()
</script>

<template>
  <section>
    <h3>{{ $t('tour.next_dates') }}</h3>
    <ul>
      <li v-for="t in termine" :key="t.id">
        <span>{{ t.date_from }} – {{ t.date_to }}</span>
        <span v-if="t.verfuegbare_plaetze === 0">{{ $t('booking.sold_out') }}</span>
        <span v-else-if="t.verfuegbare_plaetze != null">{{ $t('booking.places_left', { count: t.verfuegbare_plaetze }) }}</span>
        <p v-if="t.hinweis">{{ t.hinweis }}</p>
      </li>
    </ul>
  </section>
</template>
```

- [ ] **Step 2: Buchung-Komponenten**

[Buchung/BuchungForm.vue](../../../app/components/Buchung/BuchungForm.vue) — hier sind Feldlabels und Fehlermeldungen hardcoded. Die Zod-Schema-Messages bleiben vorerst (Phase 6), aber Labels auf `$t()`:

```vue
<Label>{{ $t('booking.persons') }}</Label>
<Label>{{ $t('booking.first_name') }}</Label>
<Label>{{ $t('booking.last_name') }}</Label>
<Label>{{ $t('booking.email') }}</Label>
<Label>{{ $t('booking.phone') }}</Label>
<Label>{{ $t('booking.notes') }}</Label>
<Button type="submit">{{ $t('booking.submit_booking') }}</Button>
<p>{{ $t('booking.select_date') }}</p>
```

[Buchung/BuchungStatusBadge.vue](../../../app/components/Buchung/BuchungStatusBadge.vue):

```vue
<script setup lang="ts">
defineProps<{ status: 'angefragt' | 'bestaetigt' | 'storniert' | 'abgelehnt' | 'durchgefuehrt' }>()
const { t } = useI18n()
</script>

<template>
  <Badge :class="`badge-${status}`">{{ t(`booking.status.${status}`) }}</Badge>
</template>
```

[Buchung/BuchungCard.vue](../../../app/components/Buchung/BuchungCard.vue) und [Buchung/BuchungDetail.vue](../../../app/components/Buchung/BuchungDetail.vue): Labels wie „Gesamtpreis", „Personen", „Wunschdatum" auf `$t()`.

- [ ] **Step 3: Build-Gate + Commit**

```bash
cd backup && yarn build 2>&1 | tail -5
git add backup/app/components/Tour/ backup/app/components/Buchung/
git commit -m "feat(i18n): localize Tour and Buchung components"
```

### Task 19: Restliche Pages auf `$t()`

**Files:**
- Modify: `app/pages/index.vue`
- Modify: `app/pages/static.vue`
- Modify: `app/pages/anmelden.vue`
- Modify: `app/pages/registrieren.vue`
- Modify: `app/pages/passwort-vergessen.vue`
- Modify: `app/pages/passwort-ruecksetzen.vue`
- Modify: `app/pages/touren/[slug]/index.vue`
- Modify: `app/pages/touren/[slug]/buchen.vue`
- Modify: `app/pages/[...slug].vue`

- [ ] **Step 1: Home (`index.vue`)**

```vue
<script setup lang="ts">
const { t, locale } = useI18n()
const { data: page } = await useFetch('/api/content/homepage', { query: { locale } })

useSeoMeta({
  title: () => page.value?.title ? `${page.value.title} | Alpenpfad` : 'Alpenpfad',
  description: () => page.value?.seo?.meta_description ?? t('footer.brand_claim'),
})
</script>

<template>
  <NuxtLayout>
    <ContentBlockBuilder v-if="page?.blocks" :blocks="page.blocks" />
    <div v-else>{{ $t('common.empty_list') }}</div>
  </NuxtLayout>
</template>
```

- [ ] **Step 2: Auth-Pages — `definePageMeta.i18n.paths` kommt in Phase 4**

Jetzt nur `$t()`-Umbau:

```vue
<!-- backup/app/pages/anmelden.vue -->
<script setup lang="ts">
const { t } = useI18n()
useSeoMeta({ title: () => `${t('auth.login')} | Alpenpfad` })
</script>

<template>
  <NuxtLayout>
    <h1>{{ $t('auth.login') }}</h1>
    <p>{{ $t('auth.login_sub') }}</p>
    <AuthLoginForm />
  </NuxtLayout>
</template>
```

Analog für [registrieren.vue](../../../app/pages/registrieren.vue), [passwort-vergessen.vue](../../../app/pages/passwort-vergessen.vue), [passwort-ruecksetzen.vue](../../../app/pages/passwort-ruecksetzen.vue).

- [ ] **Step 3: `touren/[slug]/index.vue` — Slug noch nicht lokalisiert**

```vue
<script setup lang="ts">
const { t, locale } = useI18n()
const route = useRoute()
const { data: tour } = await useFetch('/api/content/tour', {
  query: { slug: route.params.slug, locale },
})

if (!tour.value) throw createError({ statusCode: 404, statusMessage: 'Tour not found' })

useSeoMeta({
  title: () => `${tour.value?.title} | Alpenpfad`,
  description: () => tour.value?.intro,
})
</script>

<template>
  <NuxtLayout>
    <TourHero :title="tour?.title" :subtitle="tour?.subtitle" :gallery="tour?.gallery" />
    <TourFactsBar
      :duration="tour?.duration"
      :difficulty="tour?.difficulty"
      :distance="tour?.distance"
      :ascent="tour?.ascent"
      :group-size-max="tour?.group_size_max"
      :price-from="tour?.price_from"
    />
    <TourHighlights v-if="tour?.highlights?.length" :items="tour.highlights" />
    <TourIncluded v-if="tour?.included?.length || tour?.not_included?.length"
      :included="tour?.included" :not-included="tour?.not_included" />
    <TourOrganizational
      :meeting-point="tour?.meeting_point"
      :season="tour?.season"
    />
    <TourTermine v-if="tour?.termine?.length" :termine="tour.termine" />
    <TourCTA :booking-url="tour?.booking_url" />
    <TourStickyMobileCTA :booking-url="tour?.booking_url" />
  </NuxtLayout>
</template>
```

- [ ] **Step 4: `touren/[slug]/buchen.vue`**

```vue
<script setup lang="ts">
definePageMeta({ middleware: 'auth' })
const { t, locale } = useI18n()
const route = useRoute()
const { data: tour } = await useFetch('/api/content/tour', {
  query: { slug: route.params.slug, locale },
})
useSeoMeta({ title: () => `${t('booking.book_now')} – ${tour.value?.title} | Alpenpfad` })
</script>

<template>
  <NuxtLayout>
    <h1>{{ $t('booking.book_now') }} – {{ tour?.title }}</h1>
    <BuchungForm v-if="tour" :tour="tour" />
  </NuxtLayout>
</template>
```

- [ ] **Step 5: `[...slug].vue` Catch-all**

```vue
<script setup lang="ts">
const { locale } = useI18n()
const route = useRoute()
const slug = Array.isArray(route.params.slug) ? route.params.slug.join('/') : route.params.slug
const { data: page, error } = await useFetch('/api/content/page', {
  query: { slug, locale },
})
if (error.value || !page.value) throw createError({ statusCode: 404, statusMessage: 'Page not found' })

useSeoMeta({ title: () => `${page.value?.title} | Alpenpfad` })
</script>

<template>
  <NuxtLayout>
    <ContentBlockBuilder v-if="page?.blocks" :blocks="page.blocks" />
  </NuxtLayout>
</template>
```

- [ ] **Step 6: `static.vue`**

Falls noch als Fallback benutzt, analog anpassen. Falls obsolet, nicht anfassen.

- [ ] **Step 7: Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
for path in /de/ /en/ /de/anmelden /en/anmelden /de/touren/koenigssee-rundweg /en/touren/koenigssee-loop; do
  echo "$path → $(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000$path)"
done
```
Erwartet: alle `200`. **Hinweis:** `/en/touren/koenigssee-loop` funktioniert bereits, weil Server-API EN-Slug akzeptiert — Route-Path ist noch `/en/touren/…` (Slug-Lokalisierung in Phase 4 auf `/en/tours/…`).

- [ ] **Step 8: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/app/pages/
git commit -m "feat(i18n): localize all pages with \$t() and translated SEO metadata"
```

## Phase 4 — Slug-Translation + 301-Redirects

Pages bekommen per Sprache unterschiedliche URL-Pfade (`/de/anmelden` vs. `/en/login`). Tour-Detail-Routen ziehen den Slug pro Sprache aus der Directus-Translation. Alte, unprefixed DE-URLs redirecten dauerhaft nach `/de/...`.

### Task 20: Statische Pages mit `definePageMeta.i18n.paths`

**Files:**
- Modify: `app/pages/anmelden.vue`
- Modify: `app/pages/registrieren.vue`
- Modify: `app/pages/passwort-vergessen.vue`
- Modify: `app/pages/passwort-ruecksetzen.vue`
- Modify: `app/pages/konto/index.vue`
- Modify: `app/pages/konto/passwort.vue`
- Modify: `app/pages/konto/buchungen/index.vue`
- Modify: `app/pages/konto/buchungen/[id].vue`
- Modify: `app/pages/touren/[slug]/buchen.vue`

- [ ] **Step 1: Pfad-Mapping ergänzen**

Jede Page oben im `<script setup>` erhält eine `definePageMeta`-Section mit `i18n.paths`. Beispiel [anmelden.vue](../../../app/pages/anmelden.vue):

```ts
definePageMeta({
  i18n: {
    paths: {
      de: '/anmelden',
      en: '/login',
    },
  },
})
```

Kompletter Mapping-Satz (jeweils in die entsprechende Page eintragen):

| File | DE-Pfad | EN-Pfad |
|---|---|---|
| `pages/anmelden.vue` | `/anmelden` | `/login` |
| `pages/registrieren.vue` | `/registrieren` | `/register` |
| `pages/passwort-vergessen.vue` | `/passwort-vergessen` | `/forgot-password` |
| `pages/passwort-ruecksetzen.vue` | `/passwort-ruecksetzen` | `/reset-password` |
| `pages/konto/index.vue` | `/konto` | `/account` |
| `pages/konto/passwort.vue` | `/konto/passwort` | `/account/password` |
| `pages/konto/buchungen/index.vue` | `/konto/buchungen` | `/account/bookings` |
| `pages/konto/buchungen/[id].vue` | `/konto/buchungen/:id` | `/account/bookings/:id` |
| `pages/touren/[slug]/buchen.vue` | `/touren/:slug/buchen` | `/tours/:slug/book` |

- [ ] **Step 2: Interne Links updaten**

Alle `NuxtLink`/`a href`-Verweise auf diese Pages müssen jetzt `useLocalePath()` nutzen:

```bash
cd backup && grep -rn "href=\"/\(anmelden\|registrieren\|passwort-vergessen\|passwort-ruecksetzen\|konto\)" app/
```

In jeder Fundstelle — egal ob in Komponenten oder Pages:

```vue
<!-- Vorher -->
<NuxtLink to="/anmelden">Anmelden</NuxtLink>
<a href="/konto">Mein Konto</a>

<!-- Nachher -->
<NuxtLink :to="localePath('/anmelden')">{{ $t('auth.login') }}</NuxtLink>
<NuxtLink :to="localePath('/konto')">{{ $t('auth.account') }}</NuxtLink>
```

In `<script setup>`:
```ts
const localePath = useLocalePath()
```

- [ ] **Step 3: Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
for path in /de/anmelden /en/login /de/konto /en/account /de/konto/buchungen /en/account/bookings; do
  echo "$path → $(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000$path)"
done
for old in /en/anmelden /en/konto; do
  echo "$old → $(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000$old)"
done
```
Erwartet: Alle neuen Pfade `200`. Alte englische Pfade (`/en/anmelden`) **404** (keine Route registriert in EN).

- [ ] **Step 4: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/app/pages/ backup/app/components/
git commit -m "feat(i18n): translate static page paths (/en/login, /en/account, …) + localize internal links"
```

### Task 21: Tour-Slug-Translation + dynamische Page-Routes + Legacy-Redirects

Die Datei-Route bleibt `/touren/[slug]/index.vue`, aber das i18n-Modul mappt pro Locale auf `/touren/:slug` vs. `/tours/:slug`. Server-API liefert bereits den richtigen Inhalt (siehe Task 9). Zusätzlich: Alte deutsche URLs ohne `/de/`-Prefix redirecten 301 nach `/de/…`.

**Files:**
- Modify: `app/pages/touren/[slug]/index.vue`
- Modify: `app/pages/[...slug].vue`
- Create: `server/middleware/legacy-redirect.ts`

- [ ] **Step 1: Tour-Detail-Page — Pfad-Mapping**

In [backup/app/pages/touren/[slug]/index.vue](../../../app/pages/touren/[slug]/index.vue):

```ts
definePageMeta({
  i18n: {
    paths: {
      de: '/touren/:slug',
      en: '/tours/:slug',
    },
  },
})
```

Das gleiche Update für `touren/[slug]/buchen.vue` (bereits in Task 20 erledigt).

- [ ] **Step 2: Language-Switcher-Info aus API konsumieren**

Die Server-API liefert in der Response `alternate_locales: { 'de-DE': 'koenigssee-rundweg', 'en-US': 'koenigssee-loop' }`. Im Composable `useAlternateLocales` wird das global provided.

Datei [backup/app/composables/useAlternateLocales.ts](../../../app/composables/useAlternateLocales.ts):

```ts
const STATE_KEY = 'i18n-alternate-locales'

export function useAlternateLocales() {
  return useState<Record<string, string> | null>(STATE_KEY, () => null)
}

export function setAlternateLocales(map: Record<string, string> | null) {
  useAlternateLocales().value = map
}
```

In der Tour-Detail-Page setzen:

```ts
// backup/app/pages/touren/[slug]/index.vue — im <script setup>
import { setAlternateLocales } from '~/composables/useAlternateLocales'

// ... nach dem useFetch:
watchEffect(() => {
  setAlternateLocales(tour.value?.alternate_locales ?? null)
})
onBeforeUnmount(() => setAlternateLocales(null))
```

Gleiches Pattern in `[...slug].vue` und `homepage`-basierter `index.vue`.

- [ ] **Step 3: Catch-all `[...slug].vue`**

Der Slug in der URL ist bereits lokalisiert (Deutsch oder Englisch). Die API erhält ihn und matched ihn gegen `pages_translations.slug` für die aktive Sprache — keine zusätzliche Logik nötig. Die `alternate_locales`-Map aus der API wird wie oben im Composable gepflegt.

```ts
// backup/app/pages/[...slug].vue — Ergänzung
import { setAlternateLocales } from '~/composables/useAlternateLocales'

watchEffect(() => {
  setAlternateLocales(page.value?.alternate_locales ?? null)
})
onBeforeUnmount(() => setAlternateLocales(null))
```

- [ ] **Step 4: Legacy-Redirect-Middleware**

Datei [backup/server/middleware/legacy-redirect.ts](../../../server/middleware/legacy-redirect.ts) — redirected alle non-prefixed Pfade auf `/de/...` (permanent 301). Nuxt i18n macht automatisch 302 von `/` auf `/de/`; für tiefere URLs wie `/touren/...` (die vor der i18n-Umstellung von Nutzern gebookmarkt wurden) brauchen wir eigene Logik:

```ts
export default defineEventHandler((event) => {
  const url = getRequestURL(event)
  const path = url.pathname
  // Skip static assets, api, _nuxt
  if (path.startsWith('/_nuxt/') || path.startsWith('/api/') || path.startsWith('/__') || path.match(/\.(svg|png|jpg|ico|css|js|json|xml|webp)$/)) {
    return
  }
  // Skip already-prefixed paths
  if (path === '/' || path.startsWith('/de/') || path.startsWith('/en/') || path === '/de' || path === '/en') {
    return
  }
  // Matches bare path without locale → permanent redirect to DE
  return sendRedirect(event, `/de${path}`, 301)
})
```

- [ ] **Step 5: Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
# Tour slugs
for url in \
  "http://localhost:3000/de/touren/koenigssee-rundweg" \
  "http://localhost:3000/en/tours/koenigssee-loop" \
  "http://localhost:3000/de/touren/drei-zinnen-umrundung" \
  "http://localhost:3000/en/tours/tre-cime-loop"; do
  echo "$url → $(curl -s -o /dev/null -w '%{http_code}' "$url")"
done
# Legacy redirects
for url in \
  "http://localhost:3000/touren/koenigssee-rundweg" \
  "http://localhost:3000/konto"; do
  echo "$url → $(curl -s -o /dev/null -w '%{http_code} Location %{redirect_url}' "$url")"
done
```
Erwartet: Tour-URLs alle `200`. Legacy-Redirects `301` mit `/de/...` im Location.

- [ ] **Step 6: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/app/pages/touren/ backup/app/pages/\[...slug\].vue \
        backup/app/composables/useAlternateLocales.ts \
        backup/server/middleware/legacy-redirect.ts
git commit -m "feat(i18n): slug translation for tours + legacy 301 redirects + alternate_locales state"
```

## Phase 5 — Navigation aus Directus + Header/Footer-Umbau

Die hardcoded Navigation in [TheHeader.vue](../../../app/components/sections/TheHeader.vue) und [TheFooter.vue](../../../app/components/sections/TheFooter.vue) wird durch Directus-getriebene Komponenten unter [components/Website/](../../../app/components/Website/) ersetzt. Die vorhandenen Website-Komponenten werden auf den aktuellen Stack umgestellt (Server-API statt `nuxt-directus`, kein Pinia, kein `facilities`).

### Task 22: Navigation-Composable + LanguageSwitcher

**Files:**
- Create: `app/composables/useNavigation.ts`
- Create: `app/components/Website/LanguageSwitcher.vue`

- [ ] **Step 1: Navigation-Composable**

Datei [backup/app/composables/useNavigation.ts](../../../app/composables/useNavigation.ts):

```ts
type NavItem = {
  id: number
  sort: number | null
  type: string
  url: string | null
  open_in_new_tab: boolean
  title: string
  href: string
  children: NavItem[]
}

export async function useNavigation() {
  const { locale } = useI18n()
  const { data } = await useFetch<Record<string, NavItem[]>>('/api/content/navigation', {
    query: { locale },
    key: `navigation-${locale.value}`,
  })
  return {
    main: computed(() => data.value?.Main ?? []),
    footer: computed(() => data.value?.Footer ?? []),
    footerLegal: computed(() => data.value?.FooterLegal ?? []),
  }
}
```

- [ ] **Step 2: LanguageSwitcher-Komponente**

Datei [backup/app/components/Website/LanguageSwitcher.vue](../../../app/components/Website/LanguageSwitcher.vue):

```vue
<script setup lang="ts">
const { locale, locales, t } = useI18n()
const switchLocalePath = useSwitchLocalePath()
const alternateLocales = useAlternateLocales()

type LocaleObject = { code: string; name?: string; iso?: string }
const available = computed<LocaleObject[]>(() => (locales.value as LocaleObject[]))

function isAvailable(code: string): boolean {
  const alt = alternateLocales.value
  if (!alt) return true
  // Map short code to iso key (de → de-DE, en → en-US)
  const iso = available.value.find((l) => l.code === code)?.iso
  return !iso || !!alt[iso]
}
</script>

<template>
  <div class="flex items-center gap-3 text-sm">
    <template v-for="l in available" :key="l.code">
      <NuxtLink
        v-if="isAvailable(l.code)"
        :to="switchLocalePath(l.code)"
        :class="l.code === locale ? 'font-semibold text-primary' : 'text-foreground/70 hover:text-primary'"
      >
        {{ l.code.toUpperCase() }}
      </NuxtLink>
      <span
        v-else
        :title="t('common.not_available_in_language')"
        class="cursor-not-allowed text-foreground/30"
      >
        {{ l.code.toUpperCase() }}
      </span>
    </template>
  </div>
</template>
```

- [ ] **Step 3: Build-Gate + Commit**

```bash
cd backup && yarn build 2>&1 | tail -5
git add backup/app/composables/useNavigation.ts backup/app/components/Website/LanguageSwitcher.vue
git commit -m "feat(i18n): add navigation composable + language switcher component"
```

### Task 23: `components/Website/Header.vue` + `MainMenu.vue` + `MenuItem.vue` umbauen

Die drei Website-Komponenten sind aktuell inkompatibel mit dem Stack (nutzen `useDirectusItems` aus `nuxt-directus`-Modul, referenzieren einen Pinia-Store `~~/store/store`, erwarten eine `facilities`-Collection, haben eine fehlende `WebsiteBottomMenu`-Komponente-Referenz). Wir schreiben sie neu.

**Files:**
- Modify: `app/components/Website/Header.vue`
- Modify: `app/components/Website/MainMenu.vue`
- Modify: `app/components/Website/MenuItem.vue`

- [ ] **Step 1: Header.vue**

Überschreibe [backup/app/components/Website/Header.vue](../../../app/components/Website/Header.vue):

```vue
<script setup lang="ts">
const { main } = await useNavigation()
const { isLoggedIn, user } = useUser()
const { logout } = useAuth()
const localePath = useLocalePath()
const router = useRouter()

const mobileOpen = ref(false)
const scrolled = ref(false)

const onScroll = () => { scrolled.value = window.scrollY > 40 }
const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') mobileOpen.value = false }

onMounted(() => {
  window.addEventListener('scroll', onScroll, { passive: true })
  window.addEventListener('keydown', onKey)
  onScroll()
})
onBeforeUnmount(() => {
  window.removeEventListener('scroll', onScroll)
  window.removeEventListener('keydown', onKey)
  if (typeof document !== 'undefined') document.documentElement.style.overflow = ''
})
watch(mobileOpen, (open) => {
  if (typeof document === 'undefined') return
  document.documentElement.style.overflow = open ? 'hidden' : ''
})

async function onMobileLogout() {
  mobileOpen.value = false
  await logout()
  await router.push(localePath('/'))
}
</script>

<template>
  <header
    class="fixed inset-x-0 top-0 z-50 transition-all duration-300"
    :class="scrolled || mobileOpen ? 'border-b border-border bg-background/80 backdrop-blur-md' : 'bg-transparent'"
  >
    <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8">
      <NuxtLink :to="localePath('/')" class="flex items-center gap-2">
        <div class="flex h-9 w-9 items-center justify-center rounded-lg bg-primary text-primary-foreground">
          <IllustrationsIconPeak class="h-5 w-5" />
        </div>
        <span class="font-heading text-xl font-semibold text-foreground">{{ $t('nav.site_title') }}</span>
      </NuxtLink>

      <WebsiteMainMenu :items="main" class="hidden md:flex" />

      <div class="hidden items-center gap-3 md:flex">
        <WebsiteLanguageSwitcher />
        <template v-if="isLoggedIn">
          <AuthUserMenu />
        </template>
        <template v-else>
          <Button variant="ghost" size="sm" as-child>
            <NuxtLink :to="localePath('/anmelden')">{{ $t('auth.login') }}</NuxtLink>
          </Button>
        </template>
        <Button size="sm" as-child class="bg-primary text-primary-foreground hover:bg-primary/90">
          <NuxtLink :to="localePath('/#touren')">{{ $t('cta.book_tour') }}</NuxtLink>
        </Button>
      </div>

      <button
        class="inline-flex h-10 w-10 items-center justify-center rounded-lg text-foreground md:hidden"
        :aria-label="mobileOpen ? $t('nav.toggle_close') : $t('nav.toggle_open')"
        :aria-expanded="mobileOpen"
        @click="mobileOpen = !mobileOpen"
      >
        <svg class="h-6 w-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>

    <Teleport to="body">
      <Transition
        enter-active-class="transition-transform duration-500 ease-[cubic-bezier(0.32,0.72,0,1)]"
        enter-from-class="translate-x-full"
        enter-to-class="translate-x-0"
        leave-active-class="transition-transform duration-400 ease-in"
        leave-from-class="translate-x-0"
        leave-to-class="translate-x-full"
      >
        <aside
          v-if="mobileOpen"
          class="fixed inset-0 z-50 flex flex-col overflow-y-auto bg-background md:hidden"
          role="dialog"
          aria-modal="true"
          :aria-label="$t('nav.main_navigation')"
        >
          <div class="flex items-center justify-between border-b border-border px-6 py-5">
            <div class="flex items-center gap-2">
              <div class="flex h-9 w-9 items-center justify-center rounded-lg bg-primary text-primary-foreground">
                <IllustrationsIconPeak class="h-5 w-5" />
              </div>
              <span class="font-heading text-lg font-semibold">{{ $t('nav.site_title') }}</span>
            </div>
            <button
              class="inline-flex h-10 w-10 items-center justify-center rounded-lg transition-colors hover:bg-muted"
              :aria-label="$t('nav.toggle_close')"
              @click="mobileOpen = false"
            >
              <svg class="h-6 w-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 6l12 12M18 6l-12 12" />
              </svg>
            </button>
          </div>

          <WebsiteMainMenu :items="main" class="flex flex-col px-6 py-6" mobile @navigate="mobileOpen = false" />

          <div class="mt-auto flex flex-col gap-3 border-t border-border px-6 py-6">
            <WebsiteLanguageSwitcher class="justify-center" />
            <template v-if="isLoggedIn">
              <NuxtLink
                :to="localePath('/konto')"
                class="inline-flex items-center gap-3 rounded-lg border border-border px-4 py-3 transition-colors hover:bg-muted"
                @click="mobileOpen = false"
              >
                <AuthUserAvatar :user="user" />
                <span class="font-medium">{{ user?.first_name || user?.email }}</span>
              </NuxtLink>
              <Button variant="ghost" size="lg" class="h-12 w-full justify-center text-base" @click="onMobileLogout">
                {{ $t('auth.logout') }}
              </Button>
            </template>
            <template v-else>
              <Button variant="ghost" size="lg" class="h-12 w-full justify-center text-base" as-child>
                <NuxtLink :to="localePath('/anmelden')">{{ $t('auth.login') }}</NuxtLink>
              </Button>
            </template>
            <Button size="lg" class="h-12 w-full justify-center bg-primary text-base" as-child @click="mobileOpen = false">
              <NuxtLink :to="localePath('/#touren')">{{ $t('cta.book_tour') }}</NuxtLink>
            </Button>
          </div>
        </aside>
      </Transition>
    </Teleport>
  </header>
</template>
```

- [ ] **Step 2: MainMenu.vue**

Überschreibe [backup/app/components/Website/MainMenu.vue](../../../app/components/Website/MainMenu.vue):

```vue
<script setup lang="ts">
defineProps<{
  items: Array<{ id: number; title: string; href: string; open_in_new_tab: boolean; children: any[] }>
  mobile?: boolean
}>()
defineEmits<{ (e: 'navigate'): void }>()
</script>

<template>
  <nav :aria-label="$t('nav.main_navigation')">
    <ul :class="mobile ? 'flex flex-col gap-2' : 'flex items-center gap-8'">
      <WebsiteMenuItem
        v-for="item in items"
        :key="item.id"
        :item="item"
        :mobile="mobile"
        @navigate="$emit('navigate')"
      />
    </ul>
  </nav>
</template>
```

- [ ] **Step 3: MenuItem.vue**

Überschreibe [backup/app/components/Website/MenuItem.vue](../../../app/components/Website/MenuItem.vue):

```vue
<script setup lang="ts">
const props = defineProps<{
  item: { id: number; title: string; href: string; open_in_new_tab: boolean; children: any[] }
  mobile?: boolean
}>()
defineEmits<{ (e: 'navigate'): void }>()

const open = ref(false)
const hasChildren = computed(() => props.item.children?.length > 0)
const isExternal = computed(() => props.item.href.startsWith('http'))
</script>

<template>
  <li :class="mobile ? 'border-b border-border last:border-0' : ''">
    <template v-if="!hasChildren">
      <NuxtLink
        v-if="!isExternal"
        :to="item.href"
        :target="item.open_in_new_tab ? '_blank' : undefined"
        :class="mobile ? 'block py-4 font-heading text-2xl' : 'text-sm font-medium text-foreground/80 hover:text-primary transition-colors'"
        @click="$emit('navigate')"
      >
        {{ item.title }}
      </NuxtLink>
      <a
        v-else
        :href="item.href"
        :target="item.open_in_new_tab ? '_blank' : undefined"
        rel="noopener"
        :class="mobile ? 'block py-4 font-heading text-2xl' : 'text-sm font-medium text-foreground/80 hover:text-primary transition-colors'"
        @click="$emit('navigate')"
      >
        {{ item.title }}
      </a>
    </template>

    <template v-else>
      <button
        type="button"
        class="flex w-full items-center justify-between py-4 font-heading text-2xl"
        :aria-expanded="open"
        @click="open = !open"
      >
        {{ item.title }}
        <svg class="h-5 w-5 transition-transform" :class="{ 'rotate-180': open }" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
        </svg>
      </button>
      <ul v-show="open" class="pb-2 pl-4">
        <li v-for="child in item.children" :key="child.id" class="py-1">
          <NuxtLink
            :to="child.href"
            :target="child.open_in_new_tab ? '_blank' : undefined"
            class="block text-base text-foreground/70 hover:text-primary"
            @click="$emit('navigate')"
          >
            {{ child.title }}
          </NuxtLink>
        </li>
      </ul>
    </template>
  </li>
</template>
```

- [ ] **Step 4: Build-Gate + Commit**

```bash
cd backup && yarn build 2>&1 | tail -5
git add backup/app/components/Website/Header.vue \
        backup/app/components/Website/MainMenu.vue \
        backup/app/components/Website/MenuItem.vue
git commit -m "feat(i18n): rewrite WebsiteHeader/MainMenu/MenuItem for current stack + i18n"
```

### Task 24: Footer-Komponenten (neu)

**Files:**
- Create: `app/components/Website/Footer.vue`
- Create: `app/components/Website/FooterColumn.vue`

- [ ] **Step 1: FooterColumn.vue**

Datei [backup/app/components/Website/FooterColumn.vue](../../../app/components/Website/FooterColumn.vue):

```vue
<script setup lang="ts">
defineProps<{
  title: string
  items: Array<{ id: number; title: string; href: string; open_in_new_tab: boolean }>
}>()
</script>

<template>
  <div>
    <h3 class="font-heading text-sm font-semibold uppercase tracking-widest">{{ title }}</h3>
    <ul class="mt-5 space-y-3">
      <li v-for="it in items" :key="it.id">
        <NuxtLink
          :to="it.href"
          :target="it.open_in_new_tab ? '_blank' : undefined"
          class="text-sm text-background/70 transition-colors hover:text-accent"
        >
          {{ it.title }}
        </NuxtLink>
      </li>
    </ul>
  </div>
</template>
```

- [ ] **Step 2: Footer.vue**

Datei [backup/app/components/Website/Footer.vue](../../../app/components/Website/Footer.vue):

```vue
<script setup lang="ts">
const { footer, footerLegal } = await useNavigation()
const localePath = useLocalePath()
const currentYear = new Date().getFullYear()
</script>

<template>
  <footer class="bg-foreground text-background">
    <div class="mx-auto max-w-7xl px-4 py-f-20 sm:px-6 lg:px-8">
      <div class="grid gap-12 lg:grid-cols-[1.5fr_3fr]">
        <div>
          <NuxtLink :to="localePath('/')" class="flex items-center gap-2">
            <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-primary text-primary-foreground">
              <IllustrationsIconPeak class="h-6 w-6" />
            </div>
            <span class="font-heading text-2xl font-semibold">{{ $t('nav.site_title') }}</span>
          </NuxtLink>
          <p class="mt-5 max-w-sm text-sm leading-relaxed text-background/70">
            {{ $t('footer.brand_claim') }}
          </p>
          <div class="mt-6 flex gap-3" :aria-label="$t('footer.social_aria')">
            <a
              v-for="sn in ['IG', 'FB', 'YT']"
              :key="sn"
              href="#"
              class="flex h-10 w-10 items-center justify-center rounded-lg border border-background/20 text-sm font-semibold transition-colors hover:border-accent hover:text-accent"
              :aria-label="sn"
            >
              {{ sn }}
            </a>
          </div>
        </div>

        <div class="grid grid-cols-2 gap-8 sm:grid-cols-4">
          <WebsiteFooterColumn
            v-for="col in footer"
            :key="col.id"
            :title="col.title"
            :items="col.children"
          />
        </div>
      </div>

      <div class="mt-16 flex flex-col items-start justify-between gap-4 border-t border-background/15 pt-8 text-sm text-background/60 sm:flex-row sm:items-center">
        <p>{{ $t('footer.copyright', { year: currentYear }) }}</p>
        <div class="flex gap-6">
          <NuxtLink
            v-for="legal in footerLegal"
            :key="legal.id"
            :to="legal.href"
            class="hover:text-accent"
          >
            {{ legal.title }}
          </NuxtLink>
        </div>
      </div>
    </div>
  </footer>
</template>
```

- [ ] **Step 3: Build-Gate + Commit**

```bash
cd backup && yarn build 2>&1 | tail -5
git add backup/app/components/Website/Footer.vue backup/app/components/Website/FooterColumn.vue
git commit -m "feat(i18n): add WebsiteFooter + FooterColumn reading from Directus navigation"
```

### Task 25: Pages auf neue Header/Footer migrieren + TheHeader/TheFooter löschen

**Files:**
- Modify: alle 13 Pages unter `app/pages/`
- Delete: `app/components/sections/TheHeader.vue`
- Delete: `app/components/sections/TheFooter.vue`

- [ ] **Step 1: Alle Pages finden, die `TheHeader`/`TheFooter` verwenden**

```bash
cd backup && grep -rln "TheHeader\|TheFooter" app/pages/
```

- [ ] **Step 2: In jeder Page ersetzen**

Pattern-Ersetzung per Edit in jeder Page (Beispiel [index.vue](../../../app/pages/index.vue)):

```vue
<!-- Vorher -->
<TheHeader />
<main>...</main>
<TheFooter />

<!-- Nachher -->
<WebsiteHeader />
<main>...</main>
<WebsiteFooter />
```

Alternative: Ein globales `app.vue` / `layouts/default.vue` anlegen, das Header und Footer einmal platziert:

```vue
<!-- backup/app/layouts/default.vue -->
<template>
  <WebsiteHeader />
  <main class="pt-24">  <!-- pt damit fixed Header nicht überlappt -->
    <slot />
  </main>
  <WebsiteFooter />
</template>
```

Und in jeder Page `definePageMeta({ layout: 'default' })` setzen (oder default = default ist automatisch). Dann die expliziten `<TheHeader />`/`<TheFooter />` aus allen Pages entfernen.

**Empfehlung:** Layout-Ansatz — einmal definiert, ergibt Drift-Schutz.

- [ ] **Step 3: Default-Layout anlegen (falls nicht vorhanden)**

```bash
cd backup && ls app/layouts/ 2>/dev/null
```

Falls kein `layouts/`-Ordner oder kein `default.vue`, [backup/app/layouts/default.vue](../../../app/layouts/default.vue) anlegen (siehe Step 2).

- [ ] **Step 4: Alle Pages aufräumen**

Pro Page entfernen:
- `<TheHeader />`
- `<TheFooter />`
- Wrapping-Divs, die nur für die alten Komponenten da waren

Die Pages rendern ab jetzt nur ihren eigenen Content (ohne Header/Footer-Markup).

- [ ] **Step 5: Alte Komponenten löschen**

```bash
cd backup && git rm app/components/sections/TheHeader.vue app/components/sections/TheFooter.vue
```

- [ ] **Step 6: Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
for path in / /de/ /en/ /de/touren/koenigssee-rundweg /en/tours/koenigssee-loop /de/anmelden /en/login; do
  code=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000$path)
  echo "$path → $code"
done
# Prüfe, dass der Footer die Directus-Nav enthält
curl -s http://localhost:3000/de/ | grep -o "Touren\|Regionen\|Unternehmen\|Service" | sort -u
```
Erwartet: Alle Routes `200`. Footer-Check gibt `Regionen`, `Service`, `Touren`, `Unternehmen` aus.

- [ ] **Step 7: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/app/layouts/default.vue backup/app/pages/
# Deletionen sind schon gestaged durch `git rm`
git commit -m "feat(i18n): switch all pages to default layout with WebsiteHeader/Footer, remove TheHeader/TheFooter"
```

## Phase 6 — Form-Validation mit `zod-i18n-map`

Alle Zod-Fehlermeldungen kommen jetzt aus den Locale-Files. Bisher hardcoded deutsche Messages in Schemas werden durch key-basierte ersetzt.

### Task 26: zod-i18n-map Plugin + Formulare umstellen

**Files:**
- Modify: `package.json`
- Create: `app/plugins/zod-i18n.ts`
- Modify: `app/components/Buchung/BuchungForm.vue`
- Modify: `app/components/Auth/LoginForm.vue`
- Modify: `app/components/Auth/RegisterForm.vue`
- Modify: `app/components/Auth/PasswordResetForm.vue`
- Modify: `app/components/Auth/PasswordResetRequestForm.vue`
- Modify: `app/components/Auth/ProfileForm.vue`
- Modify: `app/locales/de.json` (zod namespace)
- Modify: `app/locales/en.json` (zod namespace)

- [ ] **Step 1: Installation**

```bash
cd backup && yarn add zod-i18n-map
```

- [ ] **Step 2: Zod-Namespace in Locale-Files**

`zod-i18n-map` erwartet einen `zod`-Namespace mit den Zod-Error-Codes. Die Library liefert eigene Übersetzungen mit — wir können sie direkt wiederverwenden, oder einen eigenen Override pflegen, damit unsere `validation.*`-Keys konsistent bleiben.

Pragmatisch: Die Library importiert die Basistexte selbst, wir überschreiben nur die Teile, die wir selber kontrollieren wollen (z.B. `custom` Messages unserer eigenen Validators).

In [backup/app/locales/de.json](../../../app/locales/de.json) im Root-Level (neben `validation`):

```jsonc
// hinzufügen — zod-i18n-map verwendet automatisch die mitgelieferten Basistexte
// Wir ergänzen hier nur Overrides
"zod": {
  "errors": {
    "invalid_type_received_undefined": "Dieses Feld ist erforderlich."
  }
}
```

In [backup/app/locales/en.json](../../../app/locales/en.json):

```jsonc
"zod": {
  "errors": {
    "invalid_type_received_undefined": "This field is required."
  }
}
```

- [ ] **Step 3: Plugin**

Datei [backup/app/plugins/zod-i18n.ts](../../../app/plugins/zod-i18n.ts):

```ts
import { z } from 'zod'
import { zodI18nMap } from 'zod-i18n-map'

export default defineNuxtPlugin((nuxtApp) => {
  const { t } = nuxtApp.$i18n
  z.setErrorMap(zodI18nMap({ t, ns: 'zod' }))
})
```

- [ ] **Step 4: Form-Schemas anpassen**

In jedem der 6 Formulare das Zod-Schema umstellen: hardcoded Messages raus, Standard-Zod-Validators mit Defaults (die die Map übersetzt), oder eigene keys für `validation.*`:

Beispiel [Buchung/BuchungForm.vue](../../../app/components/Buchung/BuchungForm.vue):

```ts
// Vorher
const schema = z.object({
  personen_anzahl: z.number().min(1, 'Mindestens 1 Person.'),
  kontakt_email: z.string().email('Ungültige E-Mail-Adresse.'),
  kontakt_vorname: z.string().min(1, 'Pflichtfeld'),
  // ...
})

// Nachher
const { t } = useI18n()
const schema = z.object({
  personen_anzahl: z.number().min(1, { message: t('validation.min_persons') }),
  kontakt_email: z.string().email(),  // zod-i18n-map übernimmt
  kontakt_vorname: z.string().min(1),  // zod-i18n-map übernimmt
  // ...
})
```

**Wichtig:** Für dynamische Übersetzungen (die sich bei Sprachwechsel ändern sollen) müssen die Schema-Instanzen pro Sprache neu instanziiert werden. In vee-validate/Form-Setups: Schema in einen `computed()` wrappen, der auf `locale` reagiert.

```ts
const schema = computed(() => z.object({
  personen_anzahl: z.number().min(1, { message: t('validation.min_persons') }),
  // ...
}))
```

- [ ] **Step 5: Alle 6 Formulare analog anpassen**

Für jedes Formular:
1. `const { t } = useI18n()` falls noch nicht da
2. Schema in `computed(() => z.object({...}))` wrappen
3. Hardcoded Messages durch `t('validation.xyz')` oder default (von zod-i18n-map übernommen) ersetzen

- [ ] **Step 6: Smoketest**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
```

Browser öffnen:
- `http://localhost:3000/de/anmelden`: leeres E-Mail-Feld submitten → „E-Mail"-Fehler auf Deutsch
- `http://localhost:3000/en/login`: dito → Fehler auf Englisch
- Registrierung: zwei ungleiche Passwörter → „Passwörter stimmen nicht überein." / „Passwords do not match."

- [ ] **Step 7: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/package.json backup/yarn.lock \
        backup/app/plugins/zod-i18n.ts \
        backup/app/locales/de.json backup/app/locales/en.json \
        backup/app/components/Buchung/ backup/app/components/Auth/
git commit -m "feat(i18n): localize all form validation messages via zod-i18n-map"
```

## Phase 7 — SEO, Error-Page, End-to-End-Test, HANDOFF

Feinschliff: lokalisierte SEO-Meta-Tags, hreflang-Check, lokalisierte 404-Page, finaler manueller Browser-Durchlauf, HANDOFF-Update.

### Task 27: Error-Page + hreflang-Verifikation

**Files:**
- Create: `app/error.vue`
- Modify: `app/layouts/default.vue` (falls Error-Page separat gerendert wird)

- [ ] **Step 1: Error-Page**

Datei [backup/app/error.vue](../../../app/error.vue):

```vue
<script setup lang="ts">
const props = defineProps<{ error: { statusCode: number; message?: string } }>()
const { t } = useI18n()
const localePath = useLocalePath()

useSeoMeta({
  title: () => `${props.error.statusCode} - ${t('common.not_found_title')} | Alpenpfad`,
  robots: 'noindex',
})

async function goHome() {
  await clearError({ redirect: localePath('/') })
}
</script>

<template>
  <div class="flex min-h-screen flex-col">
    <WebsiteHeader />
    <main class="flex flex-1 flex-col items-center justify-center px-6 py-24 text-center">
      <p class="mb-4 font-heading text-8xl font-semibold text-primary">{{ error.statusCode }}</p>
      <h1 class="font-heading text-3xl font-semibold">{{ $t('common.not_found_title') }}</h1>
      <p class="mt-4 max-w-md text-muted-foreground">{{ $t('common.not_found_lead') }}</p>
      <Button class="mt-8" @click="goHome">{{ $t('common.home') }}</Button>
    </main>
    <WebsiteFooter />
  </div>
</template>
```

- [ ] **Step 2: 404-Test**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
for path in /de/nicht-existent /en/does-not-exist /en/tours/koenigssee-rundweg; do
  code=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000$path)
  echo "$path → $code"
done
# Check that DE 404 is rendered on /de/… and EN 404 on /en/…
curl -s http://localhost:3000/de/nicht-existent | grep -o "Seite nicht gefunden\|Page not found" | head -1
curl -s http://localhost:3000/en/does-not-exist | grep -o "Seite nicht gefunden\|Page not found" | head -1
```
Erwartet: beide `404` bzw. `200` mit 404-Inhalt; erste `Seite nicht gefunden`, zweite `Page not found`.

- [ ] **Step 3: hreflang-Tags verifizieren**

```bash
curl -s http://localhost:3000/de/touren/koenigssee-rundweg | grep -oE '<link[^>]+hreflang[^>]+/?>' | head -5
```
Erwartet: `<link rel="alternate" hreflang="de" href=".../de/touren/koenigssee-rundweg" />`, `<link rel="alternate" hreflang="en" href=".../en/tours/koenigssee-loop" />`, `<link rel="alternate" hreflang="x-default" href=".../de/..." />`.

- [ ] **Step 4: html-lang-Attribut**

```bash
curl -s http://localhost:3000/de/ | grep -oE '<html[^>]*>'
curl -s http://localhost:3000/en/ | grep -oE '<html[^>]*>'
```
Erwartet: `lang="de"` bzw. `lang="en"`.

- [ ] **Step 5: Build-Gate + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/app/error.vue
git commit -m "feat(i18n): localized error page + hreflang verification"
```

### Task 28: End-to-End-Browser-Durchlauf + HANDOFF-Update

**Files:**
- Modify: `docs/HANDOFF.md`

- [ ] **Step 1: Dev-Server starten**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12
```

Browser öffnen und Checkliste durchgehen:

- [ ] `http://localhost:3000/` → Redirect nach `/de/`
- [ ] `/de/` zeigt Homepage auf Deutsch; Header, Footer, Blocks alle deutsch
- [ ] Sprach-Switcher oben rechts klickbar, Status-Farbe zeigt DE aktiv
- [ ] Klick auf EN → Homepage auf Englisch, URL wird `/en/`
- [ ] Home-Navigation (Anchor-Links wie `/#touren`) funktioniert
- [ ] Klick auf eine Tour-Kachel: `/de/touren/koenigssee-rundweg` — alles deutsch
- [ ] Language-Switcher zeigt EN klickbar (Translation verfügbar)
- [ ] EN-Klick → `/en/tours/koenigssee-loop`, Content auf Englisch, Slug umgestellt
- [ ] Zurück zu DE: `/de/touren/koenigssee-rundweg`
- [ ] Tour ohne EN-Translation (falls eine existiert, simulieren durch temporäres Löschen einer EN-Translation) → Switcher disabled mit Tooltip
- [ ] Login-Flow DE: `/de/anmelden` → Form funktioniert, Fehlermeldungen deutsch
- [ ] Login-Flow EN: `/en/login` → dito englisch
- [ ] Registrieren DE + EN
- [ ] Profil-Seite `/de/konto` vs. `/en/account`, Avatar-Upload funktioniert
- [ ] Passwort-Ändern in beiden Sprachen
- [ ] Buchung-Form `/de/touren/koenigssee-rundweg/buchen` vs. `/en/tours/koenigssee-loop/book`, Validierungsfehler lokalisiert
- [ ] `<html lang="de">` vs. `<html lang="en">` in DevTools
- [ ] `<link rel="alternate" hreflang="de|en|x-default">` in DevTools
- [ ] Browser-Sprache auf Englisch stellen, Cookie `i18n_redirected` löschen, `/` aufrufen → landet auf `/en/`
- [ ] Cookie-Persistenz: Nach Switch auf EN, Browser schließen/öffnen, `/` → `/en/`
- [ ] Nicht-existenter Slug `/en/tours/nicht-vorhanden` → lokalisierte 404

- [ ] **Step 2: Legacy-Redirect**

- [ ] `http://localhost:3000/touren/koenigssee-rundweg` → 301 zu `/de/touren/koenigssee-rundweg`
- [ ] `http://localhost:3000/konto` → 301 zu `/de/konto`

Verifikation:
```bash
curl -s -I http://localhost:3000/touren/koenigssee-rundweg | head -3
# Erwartet: HTTP/1.1 301 Moved Permanently, Location: /de/touren/koenigssee-rundweg
```

- [ ] **Step 3: HANDOFF.md aktualisieren**

Füge am Ende von [backup/docs/HANDOFF.md](../../HANDOFF.md) einen neuen Abschnitt ein:

```markdown
### Feature 3 — Zweisprachigkeit (DE/EN, abgeschlossen)

- `languages`-Collection mit `de-DE` + `en-US`. N-Sprachen-ready: Liste dynamisch erweiterbar.
- 15 Translations-Subtables für skalare Content-Felder (touren, tour_termine, pages, seo, 10 Blocks, navigation_items).
- 7 Item-Sub-Collections (block_statsBand_items, block_benefits_items, block_testimonials_items, block_regionList_regions, block_heroBanner_trust_signals, block_imageText_buttons, block_banner_buttons) + je eigene Translations.
- Setup-Script `scripts/i18n-setup.mjs` idempotent, mit Backup vorher nach `exports/migrations/`. Rollback per `--rollback`-Flag (manuell dokumentiert).
- Navigation aus Directus gezogen: 3 Records (`Main`, `Footer`, `FooterLegal`), DE+EN Translations. Alte TheHeader.vue und TheFooter.vue entfernt.
- Frontend: `@nuxtjs/i18n@9` mit `strategy: 'prefix'`, Default-Locale `de`, Cookie-Persistenz. Slugs pro Sprache übersetzt (z.B. `/en/login` statt `/en/anmelden`). Tour-Slugs kommen aus Directus-Translations.
- Alle Zod-Formular-Validierungen lokalisiert via `zod-i18n-map`.
- Legacy-Redirects: alte unprefixed URLs → 301 auf `/de/...`.
- Scope: Transaktionale E-Mails bleiben DE bis Buchungen live gehen (out-of-scope).

**Relevante Spec + Plan:**
- `docs/superpowers/specs/2026-04-23-i18n-design.md`
- `docs/superpowers/plans/2026-04-23-i18n.md`

**Neue Konventionen für Folgearbeiten:**
- Alle UI-Strings neu hinzukommender Komponenten in `locales/de.json` + `locales/en.json` eintragen.
- Neue Content-Collections, die Text enthalten: Translations-Subtable analog zu 4.2 der i18n-Spec anlegen.
- Interne Links: immer `useLocalePath()` statt hardcoded hrefs.
- Neuer Block-Typ: in `server/api/content/block.get.ts` in `BLOCK_CONFIG` eintragen.
```

- [ ] **Step 4: Finaler Build + Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
cd backup && yarn build 2>&1 | tail -5
git add backup/docs/HANDOFF.md
git commit -m "docs(i18n): extend HANDOFF with bilingual feature notes and new conventions"
```

- [ ] **Step 5: Zusammenfassender curl-Smoketest-Suite**

```bash
cd backup && yarn dev > /tmp/dev.log 2>&1 &
sleep 12

echo "=== Root + locale redirects ==="
curl -s -o /dev/null -w "/ → %{http_code} %{redirect_url}\n" http://localhost:3000/
curl -s -o /dev/null -w "/de/ → %{http_code}\n" http://localhost:3000/de/
curl -s -o /dev/null -w "/en/ → %{http_code}\n" http://localhost:3000/en/

echo "=== Content APIs ==="
curl -s 'http://localhost:3000/api/content/tour?slug=koenigssee-rundweg&locale=de' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>console.log('DE tour title:', JSON.parse(d).title))"
curl -s 'http://localhost:3000/api/content/tour?slug=koenigssee-loop&locale=en' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>console.log('EN tour title:', JSON.parse(d).title))"
curl -s -o /dev/null -w "DE-slug on EN → %{http_code}\n" 'http://localhost:3000/api/content/tour?slug=koenigssee-rundweg&locale=en'

echo "=== Navigation ==="
curl -s 'http://localhost:3000/api/content/navigation?locale=de' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('Nav keys:', Object.keys(j)); console.log('DE Main[0]:', j.Main?.[0]?.title)})"
curl -s 'http://localhost:3000/api/content/navigation?locale=en' | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d); console.log('EN Main[0]:', j.Main?.[0]?.title)})"

echo "=== Routes ==="
for path in /de/anmelden /en/login /de/registrieren /en/register /de/konto /en/account /de/touren/koenigssee-rundweg /en/tours/koenigssee-loop /de/touren/koenigssee-rundweg/buchen /en/tours/koenigssee-loop/book; do
  echo "$path → $(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000$path)"
done

echo "=== Legacy redirects ==="
curl -s -o /dev/null -w "/touren/koenigssee-rundweg → %{http_code} %{redirect_url}\n" http://localhost:3000/touren/koenigssee-rundweg
curl -s -o /dev/null -w "/konto → %{http_code} %{redirect_url}\n" http://localhost:3000/konto
```

Erwartet:
- `/` → 302/301 nach `/de/`
- Alle DE+EN Routes 200
- `koenigssee-rundweg&locale=en` → 404
- Legacy-URLs → 301 auf `/de/`-Variante

- [ ] **Step 6: Final-Commit**

```bash
kill $(jobs -p) 2>/dev/null || taskkill //F //IM node.exe 2>/dev/null
# Alles sollte bereits committed sein; falls es noch ungestaged gibt:
git status
# Nothing to commit → Plan komplett umgesetzt.
```

---

## Anhang A — Troubleshooting

**Script bricht mit "Kunde-Policy nicht gefunden" ab:** Auth-Setup ist Voraussetzung. `yarn auth:setup` zuerst laufen lassen (siehe HANDOFF).

**Directus-SDK wirft `UNIQUE constraint failed` beim DE-Translation-Seed:** Ein vorheriger Lauf hat bereits DE-Records angelegt, aber das Script erkennt sie nicht. Fix: Delete-Statement im Directus-Admin auf die duplizierte Row, dann erneut ausführen. Debug: Filter-Logik in `migrateOneScalar` prüfen.

**`yarn build` schlägt mit `Cannot find module '@nuxtjs/i18n'` fehl:** `yarn install` erneut laufen lassen. Wenn das Modul unter `.nuxt/types` fehlt: `yarn dev` einmal starten, Types werden dort generiert.

**Smoketest auf EN-Slug gibt 500 statt 404:** Response-Schema in `tour.get.ts` prüfen — möglicherweise ist `translations` ein leeres Array statt `undefined`, und das Flatten-Pattern wirft einen anderen Fehler. Fix: defensives Null-Check in Zeile `if (!t) throw createError(...)`.

**Legacy-Redirect redirected nicht:** Middleware läuft NUR für Server-Requests; bei Client-Side-Navigation ist sie irrelevant. Für tatsächliche 301 muss das der User im Browser direkt aufrufen (echter HTTP-Request). Testbar über `curl -I`.

**Language-Switcher zeigt beide Sprachen aktiv obwohl eine Translation fehlt:** Die `alternate_locales`-Map wird von der Page nicht gesetzt. Prüfen: `setAlternateLocales(...)` in der Tour-Detail-Page nach dem `useFetch`.

## Anhang B — Rollback-Strategie

Bei kritischen Fehlern in den einzelnen Phasen:

- **Während Phase 1** (Script fehlgeschlagen, Schema inkonsistent): `node scripts/i18n-setup.mjs --rollback` (im Script implementieren, falls noch nicht vorhanden — liest die Backup-JSONs und stellt die Haupttabellen-Spalten + -Daten wieder her). Manuelle Alternative: Directus-Admin → betroffene Collections öffnen, `_translations`-Subtables löschen, migrierte Felder per SQL-Import aus `exports/migrations/*.json` wiederherstellen.

- **Während Phase 2–7**: Git-Revert des Phasen-Commits. Der Build muss nach jedem Revert-Commit grün sein (pro Phase ist das garantiert).

- **Nach Go-Live**: Jede Phase ist einzeln git-revertbar. Reihenfolge umgekehrt zur Implementierung (Phase 7 → Phase 6 → …).

